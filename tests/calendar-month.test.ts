import test from 'node:test';import assert from 'node:assert/strict';
import {calendarMonthDays,moveCalendarMonth,calendarDay,calendarEventsOnDay} from '../src/lib/calendar-month.ts';
import type {CalendarEntry} from '../src/lib/calendar-feed.ts';
const event=(starts_at:string,ends_at:string,all_day=false):CalendarEntry=>({id:'fixture',title:'Test',starts_at,ends_at,all_day,join_url:null,source:'nbc'});
test('month layout always contains42 unique days, leap day and adjacent months',()=>{const days=calendarMonthDays('2028-02-01');assert.equal(days.length,42);assert.equal(new Set(days).size,42);assert(days.includes('2028-02-29'));assert.equal(new Date(days[0]+'T12:00:00Z').getUTCDay(),0);assert.equal(moveCalendarMonth('2026-12-01',1),'2027-01-01');assert.equal(moveCalendarMonth('2027-01-01',-1),'2026-12-01');});
test('timed sessions map to member timezone across UTC midnight and DST',()=>{assert.equal(calendarDay(new Date('2026-09-16T01:00:00Z'),'America/New_York'),'2026-09-15');const e=event('2026-03-08T06:30:00Z','2026-03-08T07:30:00Z');assert.equal(calendarEventsOnDay([e],'2026-03-08','America/New_York').length,1);assert.equal(calendarEventsOnDay([e],'2026-03-07','America/New_York').length,0);});
test('all-day and overnight intervals retain civil dates and exclusive end',()=>{const e=event('2026-09-15','2026-09-17',true);assert.equal(calendarEventsOnDay([e],'2026-09-15','America/Los_Angeles').length,1);assert.equal(calendarEventsOnDay([e],'2026-09-16','America/Los_Angeles').length,1);assert.equal(calendarEventsOnDay([e],'2026-09-17','America/Los_Angeles').length,0);const timed=event('2026-09-16T03:00:00Z','2026-09-16T04:00:00Z');assert.equal(calendarEventsOnDay([timed],'2026-09-15','America/New_York').length,1);assert.equal(calendarEventsOnDay([timed],'2026-09-16','America/New_York').length,0);assert.equal(calendarEventsOnDay([event('bad','bad')],'2026-09-15','UTC').length,0);});
