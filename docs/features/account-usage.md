# I06 — Profile settings, usage and NBC Credits infrastructure

2026-09-15, explicit user request. Keep NBC light/dark glass. Enlarge Next up in Academy, preserve16:9 poster/player (no stretched video), smoother drag/drop and size settling. Footer avatar/name opens Settings; persist own display name/avatar choice and onboarding business/timezone/goals/questions using existing member/onboarding records. Email/role read-only, no self-escalation. User can revisit onboarding from Settings. Initial avatar uses initials and a selectable color (no photo storage configured).

Add Usage navigation visible only to admins and API that independently enforces admin role, paginates/searches all member accounts including zero-use/new members. Member ID is canonical identity; wallet created automatically for new memberships with zero balance, backfill existing zero wallets without grants. Registry stores trustworthy server-side provider usage, optional token counts, duration, known cost, source/event key, observed time. Unknown tokens/cost are not invented. Current Ask Anas has no generation; future adapters can use recorder. Caller terminal provider results get attributed usage; retries idempotent, demos excluded. Existing native session totals can be shown separately from metered events. Preserve Caller logic aside from a narrow server usage hook; no new calls/campaigns.

Credits: keep existing atomic reserve/settle/release ledger. Add versioned draft equivalence, calculator and estimates in admin only. Proposal for discussion:1 NBC Credit=$0.01 purchasing value; cost×3 / credit value (rounded up per operation) is an illustrative margin, not active customer pricing. No automatic deductions until model/voice/data costs and commercial rate policy are confirmed. Provider credit counts are never USD or NBC credits. Separate observed usage from charged NBC credits.

Payment infrastructure: Stripe Checkout adapter as prepared default pending user's provider preference, packages inactive by default, server-priced purchase intents, server-only secrets, idempotent creation, signed webhook and atomic exactly-once paid credit posting. Validate amount/currency/session/owner and paid status. Redirect never grants credits. Checkout unavailable until actual account/keys/webhook/approved packages configured. No payments initiated or live keys configured in this task. Refund/dispute automation and commercial launch need explicit follow-up policy before enabling live packs.

Calendar: admin@nalify.marketing is user's reader account. Recommend owner shares only program calendar with dedicated NBC integration account, reader permission sufficient, not owner's password/account. Source calendar ID still unknown; cannot connect yet. Google/Stripe/ElevenLabs official documentation consulted.

Plan validation: additive SQL migration transaction rollback dry run, then apply after SQL tests; live role denial + owner-only profile updates, all members mapped, idempotent usage and payment posting/concurrency, unit normalization/credit quote/webhook validation, isolated build and desktop/mobile browser; freeze latest L01-R5 deployed196-file archive before integrating selected own changes. Recheck alias before authorized Vercel publish. No shared3000 restarts, no Git/push or other lane file reversions.

Implementation update: Settings uses initials/color avatar rather than photo upload; email/role cannot be edited. Saving updates shell identity after server revalidation. Academy has a width-dependent minimum row height (6/7/10) to fit16:9 plus text; older undersized layouts grow to this minimum, and other widgets retain independent resizing. Resize supports up to12 grid rows. Drop/resize settling uses300ms FLIP with document-space coordinates, correcting scroll-induced jumps; reduced-motion respected.

SQL `202609150060_usage_settings.sql` applied after rollback validation and transactional assertions. Five RLS tables, service-only RPCs, member avatar_tone column, automatic zero wallet initializer, caller terminal-event observer/backfill and atomic profile save. Existing3 members now have3 wallets;0 active payment packages and0 actual orders. Test accounts and synthetic credit entries removed. Caller provider data enriches a source/call-ID event; DB terminal trigger covers duration even before richer provider measurements. Tokens use irreversible_generation only; speculative initiated_generation is not added. Input/output/cache separate. LLM price is explicitly partial cost, not total voice invoice; vendor credits are never currency. Existing historical calls remain unknown for tokens until provider measurements are fetched; no inference from transcript length. Ask Anas has no token-producing generation currently; recorder supports future adapters but doesn't pretend these tools are wired. No NBC credit deductions are enabled.

Payment implementation has been tested end to end against a local Stripe HTTP fixture, including server-owned package price, retry same checkout, signature/live-mode checks, and six duplicate webhooks producing one credit entry. Eight concurrent SQL paid deliveries also credit once. This is NOT a real Stripe test-mode transaction; actual account, secrets, webhook registration and approved packages remain pending. No redirect grants credits. Checkout intents older than23h without a stored session cannot be recreated under expired provider idempotency. Paid confirmation validates stored session, owner, amount, currency and payment ID under a transaction/row lock. Refund/dispute automation is outside this prepared flow and must be configured before commercial launch.

Final publication:218-file snapshot rebased without conflicts onto C06-R2/K01-R6, deployment `dpl_FQWyUpDwXu1aQ7yN7fSjDoQwQmFT` READY at https://nbc-sales-nbc-sales.vercel.app/. Final build,16 units, browser editing desktop/mobile, real Supabase account workflows locally and HTTPS all passed. Real operator session reload/new tab/logout passed;24 served client assets scanned,4 private paths404. Full limits/contracts in I06 report.
