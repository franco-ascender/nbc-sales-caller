# Customizable Overview — I04-R1

2026-09-15. User rejects I03: app remains English, home Overview, premium dark/neon curved charts, customizable widgets/gallery, add/remove/resize/reorder drag-and-drop, consistent gapless layout, roadmap and Academy next-video widgets, collapsible sidebar and improved buttons. Asked asynchronously to clarify «MVC CRUD»; continue independent dashboard work while pending.

Implement no new dependencies: native pointer drag with touch/keyboard alternatives; row packing uses compact/wide/full proportions and fills the final row, common row heights, no reserved empty grid cells. Drag handles only in customization mode; size control and resize handle, removal and gallery, Save/Cancel/Reset. Per-account versioned validated browser-local preferences; persist only config/personal milestone text/lesson IDs, never auth or fetched content. Explicit UI Saved on this browser. No cross-device sync promised. Storage failures preserve editable UI and show warning. Dialog focus/escape, keyboard moves, reduced-motion, responsive collapse.

Replace ornamental hero with purposeful greeting and modular canvas. Widgets: metrics, neon smoothed activity chart (monotone without misleading overshoot), segmented glow donut, real onboarding plus personal roadmap milestone CRUD/completion, next Academy lesson with explicit choice from authorized saved inventories and click-to-load safe video, upcoming calendar call, recent conversations, NBC credits. All widgets removable/resizable. Empty states actionable, no fake video or assigned coach plan; personal roadmap clearly user-owned. Academy currently inventories under operator auth; keep permissions, handle unavailable/empty, never imply enrolled lessons from arbitrary first inventory. Local completion only for explicitly selected course. Valid HTTPS video sources: known YouTube/Vimeo embed mapping, direct mp4/webm playback, other valid links external only; no autoplay or server fetch. Member calendar uses existing authenticated actor workspace read.

Ownership: PlatformHome/CSS/SummaryCharts, new dashboard layout/content/editor helper components, PlatformShell and isolated shell overrides. Preserve modules, credit auth, snapshots. Current I03 omitted a concurrent S01 session deployment: integrate S01's verified three runtime files from archived source to restore already-authorized 12-hour login, not mutable worktree. Freeze I03+S01+own files, check remote again before deployment and merge only verified nonconflicting changes.

Tests: packing completeness/proportions/reorder/validation, smoothed chart geometry, roadmap CRUD preferences, safe video URLs. E2E fixture desktop/mobile: add/remove/reset/save/cancel/reload/account isolation, drag pointer/touch and keyboard, sizing/dense layout, dialogs, milestone CRUD, media activation, real empty/error, sidebar hide/show and navigation. Isolated Node24 build/server; live auth and HTTPS reads no domain writes. Record fixtures vs actual data separately. Publish under existing user authorization to the same Vercel alias, no Git push, retain report/handoff/source archive.

Integration update: C04-R1 published during I04 development. Verified archive SHA e59bf1fe8b9c97cee5576e085e8a8f7f76f6253cd439f01f331342801f1f5f50, 18 nonconflicting Caller changes now included. Overview pipeline continues canonical status categories, labelled Standard stages; custom CRM buckets remain available in Caller. New independent do_not_call flag must also exclude outreach-ready counts. Preserve S01 verified restoration. Final snapshot 142 files before helper changes.

Entregado I04-R1: 142 archivos runtime, build/TypeScript y 13 unitarias aprobadas; 4 grupos fixture y 2 recorridos reales tanto local como HTTPS. Publicado en dpl_EMHgAQ81sUFdL59T6piDcFEehAjD; reporte `docs/lanes/reports/I04.md`. Preferencias e hitos/progreso propios siguen siendo locales por navegador. API/schema sin cambios.
