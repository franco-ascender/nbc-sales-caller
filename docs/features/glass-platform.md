# I05 — NBC glass, unified appearance and direct widget editing

2026-09-15. User supplied five visual references: bright neutral cards, independent metric widgets, editing by touching blocks, corner remove/resize, a gallery previewing actual widget content with Small/Medium/Large, and Done. Keep English, NBC navy/blue/gold. Default light; optional dark persists globally across routes. Liquid glass is subtle translucency, reflected edges, blur, restrained highlights and opaque reduced-transparency fallback; no black/turquoise island.

Scope: global appearance provider/tokens and conservative CSS tokenization of literal colors for theme compatibility across modules, scoped dashboard refinements, new direct 2D widget canvas and gallery using actual data previews. Split metrics into individually removable widgets; migrate I04 layout/preferences without losing milestones/lesson selection. Small/Medium/Large presets affect width AND height, resize corner previews live, dragging whole widget in edit mode, keyboard alternatives, per-widget menu, long press to enter Edit, Done persists, Cancel discards, Restore resets layout. Dense row-major grid packs gaps from varied heights with bounded width/height; responsive smaller viewports clamp widths while retaining stored desktop sizes. DOM order follows visible packed coordinates. No fake performance values in previews.

Session: current C05 public alias tested with real auth: reload/new tab pass (one password login). Investigate rotation/stale auth event recovery; preserve fixed12h TTL, SDK refresh and server validation. Do not claim original complaint reproduced if it isn't. Add token-rotation/restore tests, no weakening authorization.

Calendar: requested provider/calendar ID/access account via async question. Current connector lists support@nbcsales.io and a holidays calendar, not a confirmed mentoring source. Do not mirror unrelated calendars or make calendar public. Prepare existing native calendar/read-only integration contract independently; actual external connection waits for identifying source and usable deployment credentials. User reader scope does not grant editing events. Keep secrets server-side and expose only member-facing title/time/join information. No calendar emails or writes.

Base: current alias C05-R1 dpl_Ayz6LH4wxxNNMWNuNvuVcJsfdQH8, verified archive cc064feae85c782cf8f377ed1c3d5bea043f572b8e7cf475b6c2d582070f9c49, 189 files includes K01-R5, L01-R4, I04 and S01. Freeze only own changes over archived deployed state; check alias before publish and reconcile newer nonconflicting verified snapshots. Other lanes' logic untouched. No new library required. Build/browser in isolated staging/port, publish same Vercel alias under authorization already given. Feature/handoff/report with explicit real/mock/pending evidence.

Implementation and local validation complete:31 runtime deltas over C05,196 total files.13 unit tests; direct mouse and touch-event drag/resize, keyboard move, gallery, persistence/cancel/restore, responsive320/390/1440 and reduced motion pass. Default rows use8+4 charts and6+6 learning cards to avoid unused initial cells. Arbitrary mixed dimensions use dense placement; unfillable residual gaps can remain when no widget fits, and smaller widgets scroll overflowing content. Preferences and personal milestones remain per-account on this browser; appearance is browser-wide.

The responsive activity SVG now measures its physical box, keeping curve, pointer coordinates and labels aligned after resizing. Charts use NBC blue/violet and gold stages; roadmap no longer uses mint. Global root appearance is initialized before hydration and saved across reload/routes. Theme tokenization converts existing module colors without changing Caller, Academy or Lead Engine logic. Explicit foreground/action tokens resolve navy-on-dark and pale-button contrast. Reduced-motion and reduced-transparency fallbacks included. Existing browser storage migration turns the old combined metrics card into four separate widgets.

Session recovery changes verified with seven controlled browser cases: expired JWT refresh without password, new tab using rotated token,503 recovery hiding private content, stale401 for an older token cannot clear a newer saved session, genuine403 clears access, logout during pending verification cannot reopen workspace, expired fixed NBC deadline requires login. Real operator login/reload/new tab/logout passed locally. Initial production immediate reload also passed before changes, so the original complaint was not reproduced; implementation hardens stale-event/rotation recovery rather than claiming an observed original root cause. The existing S01 clock-driven browser harness stalled while restoring; replaced with isolated clock-free network/token scenarios plus fixed-TTL storage unit tests. No claim of waiting12 wall-clock hours. Reference: [Supabase sessions](https://supabase.com/docs/guides/auth/sessions).

Calendar API200 confirmed with real membership locally; native feed empty for the tested account and externalStatus=not_connected. See [calendar connection contract](calendar-connection.md). No external mentoring calendar has been linked or populated. Local11-route ×2-theme real navigation passed, with two existing automatic Caller reconcile POSTs from entering Caller; no calls started, no gallery/domain edits. Screenshots distinguish DEMO fixture metrics from real empty state.

Published and verified HTTPS: dpl_46gSjX4JyRKbysXe52Ho9VXAy1AE, stable https://nbc-sales-nbc-sales.vercel.app/. Same frozen196-file manifest, Vercel READY. Actual-session restore/new tab/logout and five anonymous API protections pass.11 routes in both appearances pass with no page errors,35 client assets contain no configured private values, private files404. Exemption removes only Vercel's deployment login; NBC membership auth remains required. External mentoring calendar remains not_connected. See I05 report for exact source hashes and evidence.
