# Industry worker contract

Read `docs/features/owner-cell-industry-workflows.md` for schema and scope. Work ONLY on your assigned industry. Read its live brain entry, relevant section of handoff/04_SOURCE_CATALOG.md and handoff/07_RESEARCH_BUILD_SPEC.md, and the actual runtime functions involved. Do not ingest unrelated history.

Write only `docs/features/industries/<key>.md` and `src/data/industry-workflows/<key>.json`. If a runtime defect is found, report exact function/file and a concrete correction to root; do not edit shared files. No other writers on your two files.

Run a bounded, free 5-20 row sample when a legally eligible public business-contact endpoint is already VERIFIED in catalog or code. Only collect aggregate evidence, not personal phone/address lists. Do not use residential/property data to uncover private cell numbers. Do not run paid APIs, Apify, BatchData, Telnyx, Anthropic or appends. Do not load secrets. Do not deploy. For an unavailable or unsuitable source, document the actual limitation; fixtures/code audits are explicitly labeled, never called live measurements. Do not declare convergence or a best possible workflow without paid outcome evidence.

Each dossier is English with hypothesis 1, industry-specific failure cases, bounded tests, iterations, implementable free correction, source tags and exact paths, denominator/cost distinction and remaining experiment. In JSON use the exact contract. Graph must include meaningful accepted/rejected branches, not just a generic linear scrape/verify chain. All graph nodes reachable from the first node; node IDs short and unique. Baseline measured rate and n remain in live brain, not in this JSON. `perCleanUsd` is null until verified. Dates use actual session date. An owner candidate is not an owner-confirmed mobile.

Keep tasks bounded: one complete review iteration with actual free evidence or explicit unavailable-source result, not speculative vendor research. Return short summary, file paths, sample n and the most material runtime defect. You may not delegate further.

Do not fetch user-prohibited lists (DMV, voter, SC/UT licensee lists, WA liquor, NIPR, manufacturer locators), even if a historical adapter exists. Do not run npx commands that install dependencies. Validate with `node --experimental-strip-types`, not an auto-installing tsx invocation. Root runs shared compilation and suites once integration is complete.
