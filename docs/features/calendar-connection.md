# Calendar feed — I05

2026-09-15. Native NBC coaching sessions remain backed by the existing member workspace and its audience rules. `GET /api/calendar` requires an active member with the standard bearer token; it returns only the upcoming member-facing feed, never OAuth credentials, attendee lists, descriptions or calendar administration data. Refresh is read-only. Staff can expand the existing NBC event-management interface; its server authorization remains unchanged.

Response: `{events:[{id,title,starts_at,ends_at,join_url,all_day,source}],timezone,externalStatus,checkedAt}`. Status is `not_connected`, `connected` or `unavailable`. Private/no-store. Unauthorized401; inactive membership uses existing MemberError status; unexpected failure503. Native event IDs are prefixed `nbc:`, Google IDs `google:`. External failure preserves native events. The deployment currently returns `not_connected`; external integration has not been activated.

Prepared Google adapter, pending source confirmation: server refreshes a dedicated OAuth token and requests upcoming events for one fixed `NBC_GOOGLE_CALENDAR_ID`, expanding recurring instances through Google (`singleEvents=true`), ordered by start time, at most250 events over60days. No writes, invitations or making a calendar public. Cancelled and explicitly private events are omitted. Meeting link is a HTTPS video entry point / Hangouts link; events without one show joining-link pending. URLs embedded in event descriptions are not parsed. Feed does not paginate beyond250. All-day display supported in Calendar; Overview currently formats its next event as a timed session.

Required deployment configuration, server only: `NBC_GOOGLE_CALENDAR_ID`, `GOOGLE_CALENDAR_CLIENT_ID`, `GOOGLE_CALENDAR_CLIENT_SECRET`, `GOOGLE_CALENDAR_REFRESH_TOKEN`. No values are supplied or committed. Request OAuth offline access for a dedicated calendar-reading account with `https://www.googleapis.com/auth/calendar.events.readonly`; the account must have reader access to the confirmed program calendar. The app must be configured in Google's OAuth project and receive an authorized refresh token; an IDE connector session is not a deployable credential. Do not request passwords through chat. Prefer sharing only the program calendar with the dedicated account rather than broad personal calendar access. The API accepts no client-provided calendar IDs.

A user with reader access can supply the provider, calendar ID/link and account email; the remaining connection step is to authorize the deployment integration with read-only access. If the provider is Outlook, adapt this boundary after confirmation instead of claiming Google connectivity.

Connected Calendar app discovery in this session identified a primary support account and a holidays calendar; neither was confirmed as the mentoring source. No unrelated events were fetched or copied. User's async question remains unanswered. Native feed and failure handling can be reviewed now; actual mentoring calls cannot be verified until the source and usable read-only credentials arrive.

Official reference checked: [Google Calendar scopes](https://developers.google.com/workspace/calendar/api/auth). No schema migrations or pricing changes.

I06 update2026-09-15: User confirms reader account admin@nalify.marketing. Recommend calendar owner shares only program calendar with dedicated NBC integration identity as reader; no owner login/password needed. Still awaiting exact calendarID/link and server OAuth connection. No calendar selected or imported this turn.
