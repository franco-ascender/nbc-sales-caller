# C08 — contrato telefónico y configuración comprobada

2026-09-17 · Caller · Entrega1: contrato preparado para implementar; telefonía no habilitada.

## Resultado

Leídos CALLER-CODEX y OR04/C08, con ownership Caller. Se conservaron C01–C07, L02/T1 y archivos compartidos. [Contrato de implementación](../../features/caller-telephony.md): Twilio crea una única llamada propia y ElevenLabs conecta la voz mediante register-call. Se especifican rutas, persistencia, idempotencia, eventos autenticados, Stop, DNC, recuperación, límites y aceptación. La elección permite enviar TimeLimit al crear la llamada y conservar callbacks de Twilio; no requiere importar el número ni modificar el agente web.

El contrato evita repetir una llamada tras una respuesta perdida: el intento queda ambiguo hasta conciliación, conservando su reserva. Tampoco confunde una conexión telefónica terminada con resultado comercial o transcripción disponible. C-RECOVERY permanece reproducible por inspección del código actual y queda incluido en la siguiente implementación; no se declara corregido.

## Configuración comprobada hoy

GET autenticados con la clave local, **16:24:14 UTC**: `/v1/user`, `/v1/user/subscription`, `/v1/convai/phone-numbers`, agente configurado y `/v1/convai/settings`: los cinco respondieron200. La cuenta accesible por esa clave informa tier/status **free**, clonación instantánea/profesional no habilitada; agente existente con voz, autenticación y límite de duración, grabación desactivada. Sin número Twilio importado ni webhook post-call configurado. Este es un chequeo nuevo, no repetición del diagnóstico histórico. No demuestra que la cuenta Premium informada por Franco sea Free: falta ubicar/conectar su acceso correcto.

La revisión posterior del entorno configurado encontró credenciales Twilio. GET de cuenta e inventario, **21:57:59 UTC**, devolvió cuenta activa Full y un número existente con capacidad Voice; su identidad se mantiene privada y no se le asignó uso. No hay número importado en ElevenLabs. La ausencia de número importado no prueba ausencia de números en Twilio.

Con la clave Creator se creó un solo agente interno de piloto telefónico, sin llamadas ni clonación: voz estándar, entrada/salida μ-law8kHz, duración máxima60s, concurrencia1, límite diario1 y grabación desactivada. GET de verificación a las **21:59:27 UTC** devolvió200 y confirmó esa configuración. Su ID se guardó solo en `.env.local`; no se publica aquí. No se configuró webhook ni se vinculó el número Twilio.

El audio suministrado por Franco para Anas se inspeccionó localmente: WAV mono8kHz, 47m39s y 45.7MB. Cumple duración para Professional Voice Clone pero incluye partes de Salem; su calidad telefónica limita la fidelidad posible. Se creó un PVC privado y se cargó esa muestra sin eliminación de ruido ni entrenamiento. ElevenLabs separó dos hablantes; Franco confirmó Speaker1 como Anas y solo ese speaker quedó seleccionado. No se verificó al propietario y no se generó ni entrenó una voz. Anas deberá completar la verificación de voz del proveedor antes del entrenamiento.

GET Vercel de metadata del entorno del proyecto, **16:26:08 UTC**, respondió200: API key y agent ID de ElevenLabs registrados como sensitive para preview; ninguna variable Twilio listada. No se descifraron/verificaron credenciales desplegadas ni se modificó configuración.

## Evidencia y límites

- [Resumen saneado de comprobaciones](../../../artifacts/lanes/C08/config-check-20260917.json): sin claves, IDs privados, cuotas, teléfonos ni prompts.
- Documentación oficial y OpenAPI consultados; enlaces exactos en el contrato. GET locales ejecutados mediante Node/fetch y `@next/env`, con timeout y salida allowlist; inspección con `rg`/lecturas acotadas.
- No hubo llamadas, síntesis, clonación, compras, SQL, modificaciones del runtime ni deploy. No se ejecutaron tests de aplicación: esta entrega contiene documentación/evidencia, no implementación. La comprobación documental posterior valida JSON, enlaces locales y hashes; no acredita telefonía.
- Estados separados: cuenta Creator comprobada; número Twilio existente comprobado, sin asignar; agente telefónico preparado; webhook pendiente; voice test no realizado; clonación no realizada; llamada telefónica no realizada.

## Próximo paso

Pedir Terra/medio antes de implementar el contrato, como solicitó Franco. La implementación aislada y sus fixtures pueden avanzar mientras Infra conecta credenciales; publicar/aplicar SQL requiere coordinación y revisión del candidato. Antes del piloto faltan destino propio y presupuesto monetario concreto, además de tarifas verificadas. Se propone una sola conversación de60s y timbrado20s, todavía sin autorización para marcar. Clonación queda desacoplada: faltan aportante/consentimiento/muestra.

Modelo solicitado por Franco: Sol/alto para este contrato; selector efectivo no verificable por herramientas, comprobación solicitada. No se afirma un cambio automático. Tokens y costo de desarrollo desconocidos; [registro](../../../artifacts/lanes/C08/task-cost.json). No afirmar costo cero por no haber hecho llamadas. Revisión del orquestador y Franco pendiente; hashes en [manifest](../../../artifacts/lanes/C08/manifest.json).

## Delta de implementación posterior

En Terra/medio se corrigió C-RECOVERY en `Caller.tsx`: una recuperación solicitada mientras otra conserva el lock por renovación de token queda en cola y se ejecuta al liberar el lock; el resultado antiguo sigue descartándose por identidad de token. Se agregó el contrato verificable de callbacks telefónicos en `caller-telephony-validation.ts`: E.164, estados Twilio allowlist, firma Twilio sobre URL HTTPS/campos y HMAC+timestamp de ElevenLabs. No hay ruta de llamada ni webhook público habilitado todavía. `caller-recovery.test.ts` y `caller-telephony-validation.test.ts` pasan junto a TypeScript sin errores.

También quedó preparada la política de cobertura local: `caller-city-numbers.ts` elige exclusivamente números NBC activos por ciudad/estado, devuelve cotización o revisión para ciudades sin cobertura y no contiene ninguna capacidad de compra. La compra requiere aprobación explícita de Anas y respeta un máximo de diez números activos. Se documentó el inventario, cotización y requisitos regulatorios en `docs/features/caller-city-number-coverage.md`; `caller-city-numbers.test.ts` cubre la selección y los bloqueos. La migración candidata `202609170080_caller_city_number_coverage.sql` persiste ese inventario con RLS para service role; no se aplicó. No se consultó disponibilidad, no se compró un número y no se llamó a nadie.

Se añadió la pestaña administrativa **Local coverage** al Caller. Su endpoint autenticado sólo lee la política y el inventario; si la migración todavía no está aplicada informa ese estado y conserva todas las acciones inhabilitadas. No expone el número telefónico en la UI y no incorpora una operación Twilio de compra. TypeScript y las ocho pruebas C08 actuales pasan.

El núcleo de estados telefónicos quedó aislado en `caller-phone-state.ts`: despacho ambiguo no se convierte en rechazo, callbacks tardíos no reabren una llamada terminal y Stop se persiste antes de pedir una terminación al proveedor. También bloquea el registro de ElevenLabs después de Stop. `caller-phone-state.test.ts` añade fixtures de orden, Stop y ambigüedad; las doce pruebas C08 y TypeScript pasan. No se conectó una ruta de marcación ni se llamó a ningún número.

El 2026-09-18 se verificó por API que la voz profesional privada de Anas está entrenada para los modelos telefónicos y tiene preview disponible. Se asignó esa voz al agente telefónico de piloto y se leyó nuevamente la configuración para confirmar el cambio. No se inició una conversación, no se vinculó un número Twilio y no se produjo una llamada.

El 2026-09-21 se activó la compra controlada desde **Local coverage**: búsqueda live por ciudad/estado, precio mensual de la cuenta, aprobación exclusiva de Anas, techo mensual, capacidad, confirmación por número, claim idempotente y reconciliación de resultados ambiguos. Las credenciales Twilio se añadieron como secretos de producción en Vercel. Las migraciones `080`, `210` y `220` están aplicadas; la política inicial permanece bloqueada, presupuesto `null`, inventario vacío. La verificación read-only de Miami devolvió tres candidatos elegibles y tarifa de cuenta USD 1.15/mes. No se compró ningún número.

El 2026-09-21 se reemplazó el resguardo piloto de 60 segundos del agente telefónico por el máximo permitido por ElevenLabs: 7.200 segundos. Brain V2 quedó verificado en agentes web y telefónico con núcleo compartido, contexto por canal, GPT-4.1 Mini sin cambio, temperatura 0.35 y máximo 140 tokens. El navegador conserva 600 segundos. Se agregó un scorecard local sobre resultados guardados: concisión, preguntas apiladas, participación del prospecto, cobertura de discovery, siguiente paso, Stop e intervalo entre respuestas. No se inició una llamada telefónica.

La persistencia candidata para el piloto quedó definida en `202609180090_caller_phone_attempts.sql`: piloto aprobado con destino propio, reserva de coste y vigencia; intentos idempotentes con claim y estados separados; y eventos autenticados deduplicables de Twilio/ElevenLabs. RLS impide acceso de navegador. La migración no fue aplicada ni contiene una acción que llame, compre o facture.

Diagnóstico 2026-09-18: la aplicación web estaba configurada con el agente Telephone Pilot, por lo que su conexión de navegador falló después de reservar una autorización. Se creó un agente browser separado con la voz de Anas, autenticación de sesión, grabación desactivada y límite de 300 segundos. `ELEVENLABS_WEB_AGENT_ID` separa el agente web del telefónico en código y entorno local. TypeScript pasa. El despliegue no se realizó: el CLI autenticado de Vercel devolvió `forbidden` para el equipo seleccionado; no se modificó Vercel.
