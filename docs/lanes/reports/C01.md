# Reporte C01 — revisión 2: publicado para revisión en Vercel

Fecha: 2026-09-14, 16:45 EDT / 20:45 UTC. Estado vigente: **C01-R1 publicado en preview y verificado por HTTPS**. Esta revisión 2 actualiza publicación/evidencia; el código runtime no cambió respecto de C01-R1. Las afirmaciones de «deploy pendiente» de la revisión 1 conservada debajo son históricas y quedan reemplazadas por este bloque.

**Abrir Caller: https://nbc-sales-nbc-sales.vercel.app/caller**

URL inmutable del candidato: https://nbc-sales-76hzqvi1w-nbc-sales.vercel.app/caller
Deployment: `dpl_FPv9BGxD1u8eukenxRV3YSLXp1iW`, proyecto NBC Sales `prj_IpKvXDe4aOUOYXoVY22sEw3f2YFR`, estado **READY**, target **staging / preview**.

## Autorización posterior y alcance publicado

Franco indicó en este chat: «Okay, empeza a poner todo en el Vercel porque ahí es donde estoy revisando todo, y ahora mismo lo único que me aparece es: operator email, operator password. Así que avancemos, dale.» Esta instrucción posterior autoriza publicar el candidato C01 ya preparado para revisarlo en Vercel. No se solicitó otra autorización redundante ni se declara aprobación funcional/aceptación del producto.

Fuente: baseline I01 congelado + ocho archivos runtime C01-R1 (seis sustituciones, dos nuevos), total **59 archivos**, sin tomar modificaciones concurrentes de otros lanes. El resto de módulos mantiene el baseline publicado por I01. No se incorporaron L01/K01 ni cambios posteriores de shell; sus migraciones/revisión continúan a cargo del orquestador.

Manifiesto runtime: `artifacts/lanes/C01/vercel/runtime-manifest.json`. Identificador del contenido JSON normalizado enviado a Vercel: `6d1db36129beaa96b71b8d11083396332fd83918d5832ba9a6023aca4d88ad5c` (metadato `callerManifest`). Snapshot reproducible: `nbc-sales-baseline-plus-C01-R1.tar.gz`, ruta/hash en `runtime-archive.json`. Se verificaron todos los hashes antes del upload; cero coincidencias de valores privados en archivos subidos. No se subieron `.env`, docs, tests, artefactos ni exportaciones de DB.

Sin Git, push, merge, promoción a producción, compra, nueva dependencia, migración, cambio de auth o nueva conversación. El proyecto conserva sus seis variables runtime preview. Se usó la misma excepción de Deployment Protection de I01, aplicada exclusivamente al nuevo deployment para compartir su URL; autorización de aplicación intacta. La protección general del proyecto no se modificó.

## Acceso de Franco

Los campos **Operator email / Operator password son el login privado**, no un error de Vercel ni una pantalla incompleta. **La cuenta existente funciona**: se comprobó tanto contra el baseline como contra esta publicación con credenciales leídas solo en memoria, sin imprimirlas ni guardarlas en artefactos.

Para entrar, usar los valores locales existentes `NBC_OPERATOR_EMAIL` y `NBC_OPERATOR_INITIAL_PASSWORD` de `.env.local`. El usuario comparte esta carpeta y puede consultarlos allí; no se copian al chat/documentación. No se reseteó password, creó otra cuenta, compartió la cuenta de Franco con Anas ni envió correo. Se pidió aclaración opcional sobre si dispone de las credenciales; no es un bloqueo de publicación ni autorización para cambiar acceso.

1. Abrir el enlace actual de Caller arriba e iniciar sesión.
2. Observar **Recover pending**, historial y métricas; el login ejecuta un lote. En la cuenta comprobada había **cero pendientes**: el endpoint devolvió 200 con lote vacío. Esto valida el flujo real, no una recuperación externa de un pendiente que no existía.
3. Usar **Load more** si existe otra página. Con los pocos datos reales se comprobó paginación HTTP de tamaño uno, sin repetir IDs entre páginas. Los 65 registros y los casos de recuperación parcial siguen siendo fixtures locales claramente identificados.
4. Para revisión visual general usar la raíz del mismo dominio. Si se sigue usando la URL inmutable antigua `nbc-sales-nyqgilywg-...`, se verá todavía el baseline: abrir el enlace nuevo o el alias actual.

## Verificación realmente ejecutada en esta revisión

| Comando / prueba | Resultado | Evidencia en `artifacts/lanes/C01/vercel/` |
|---|---|---|
| API Vercel GET proyecto/deployments | Proyecto correcto; único READY anterior era I01 | Metadatos comprobados en esta sesión, sin secretos |
| Login Supabase y GET historial HTTPS baseline | 200 / 200 | `access-check-before.json` |
| `npm exec --offline --package=node@24 -- node node_modules/next/dist/bin/next build` | Exit 0; copia aislada, Node 24, build estándar y TypeScript | `build.log` |
| `node artifacts/lanes/C01/vercel/deploy-candidate.mjs` | POST /v13/deployments 200; 59 archivos exactos; cloud READY | `deployment-create.json`, `deployment-status.json` |
| Excepción de acceso solo del deployment | 302 antes, PATCH 200, URL/alias públicos después | `preview-access.json`, `verification.json` |
| `node artifacts/lanes/C01/vercel/verify-caller.mjs` | Exit 0, nueve comprobaciones HTTP/browser | `verification.json` |
| Anónimo GET sessions y POST reconcile | Ambos 401 / no-store reales | `verification.json` |
| Login real Chrome, recuperación al entrar | Éxito, POST reconcile 200, cero pendientes existentes | `verification.json` |
| Dos páginas reales `limit=1`; cursor inválido | 200 sin IDs duplicados; inválido 400 | `verification.json` |
| Móvil autenticado 390 px, logout y JS | Sin overflow; logout correcto; cero pageerrors; **cero inicios de voz** | `verification.json` |
| Captura pública antes de ingresar credenciales | Login real de esta publicación | `public-login.png`; sin capturas privadas |

Primera ejecución del verificador HTTPS falló en la segunda aserción anónima inmediatamente después de aplicar la excepción; el registro no guardó el status observado, por lo que no se atribuye una causa confirmada. Consulta siguiente del endpoint: 401/no-store; repetición completa: exit 0. `verification-initial.json` conserva el fallo. No se cambió runtime para resolverlo.

No se volvieron a atribuir como nuevas las 15 unitarias/2 E2E fixture de R1: código idéntico, evidencia de R1 conservada. La publicación se reconstruyó con el baseline exacto y verificó nuevamente build y flujo real HTTPS. No hubo pendientes reales para validar recuperación de contenido externo ni ensayo de audio; esos límites permanecen.

Referencias oficiales consultadas para ejecutar: [Create deployment](https://vercel.com/docs/rest-api/deployments/create-a-new-deployment), [Deployment Protection Exceptions](https://vercel.com/docs/deployment-protection/methods-to-bypass-deployment-protection/deployment-protection-exceptions) y OpenAPI `https://openapi.vercel.sh`. Se usó `target: staging` explícito; no se cambió plan ni protección general.

## Documentación y estado de revisión

Archivos agregados a la entrega documental: `artifacts/lanes/C01/vercel/` con snapshot, manifiesto, scripts de ejecución/verificación y logs saneados; feature, handoff y este reporte actualizados. Los scripts de esta carpeta son evidencia local, no parte del upload. Configuración global y documentos de I01 permanecen bajo su ownership.

Deltas adicionales para el orquestador: API_DOCS/arquitectura/overview/04-deployment deben registrar que **C01-R1 ya está en preview** sobre baseline I01, con las URLs/ID anteriores, sus nuevos contratos y verificación autenticada real. C01 no está integrado con L01/K01 en este deployment. CHANGELOG propuesto: **2026-09-14 16:45 EDT — CHANGED: publicar baseline + C01-R1 para revisión en Vercel tras instrucción expresa de Franco; 59 archivos, build Node24 y verificación HTTPS aprobados, auth intacta, cero nuevas conversaciones; sin push ni producción.** Request: cita anterior. Consolidación global sigue pendiente.

- Implementación C01: completada localmente, código R1 intacto.
- Revisión técnica externa del orquestador: pendiente; no se inventa aprobación.
- Autorización de Franco para publicar este preview revisable: **RECIBIDA**, mensaje posterior citado arriba.
- Aceptación funcional de Franco: pendiente, revisa en Vercel.
- Deploy preview: **EJECUTADO y VERIFICADO**.
- Push: **NO EJECUTADO**; promoción producción/auto-deploy: no ejecutados.
- Próxima tarea sugerida: revisión funcional en HTTPS y devolución de ajustes por Franco/orquestador; no iniciada automáticamente.

---

# Antecedente conservado — reporte C01 revisión 1

Fecha y entorno: 2026-09-14, 16:22 EDT / 20:22 UTC. macOS, Node 26.0.0, Next 16.3.5, TypeScript instalado en el proyecto, Chrome headless. Workspace Local compartido; build/navegador en copia temporal independiente.
Estado de implementación: **LISTO PARA REVISIÓN LOCAL**. Integración y validación remota autenticada pendientes.
Lane y herramienta: Caller / Codex Local, un escritor, sin subagentes.
Objetivo: continuidad de resultados e historial mayor a treinta sesiones, sin reconstruir Caller ni generar nuevas conversaciones.
Candidato: **C01-R1**, manifiesto `artifacts/lanes/C01/manifest.sha256` y su digest `manifest-digest.txt`. No hay repositorio Git en esta carpeta; `git status --short` devolvió «not a git repository». No hay commit inventado. El manifiesto incluye exclusivamente archivos propios y este reporte; el build usó una copia del contexto compartido que no equivale a aprobación de los otros lanes.

## Resultado

Implementado sobre las rutas y servicios reales: historial con cursor estable por fecha/UUID, límites validados, recuperación en lotes de hasta cinco, resultados individuales, expiración y protección contra escrituras concurrentes. Un lote al entrar después del login; botones para repetir y seguir con pendientes más antiguos. Ninguna conversación nueva se crea durante recuperación. No hay worker permanente ni continuidad autónoma mientras el operador está fuera.

La UI carga más páginas, conserva selección, evita peticiones dobles, mantiene resultados finales y permite reintentar fallos de páginas o sesiones. Las métricas siguen cubriendo las treinta sesiones más recientes **cargadas**; búsqueda y filtro de estado abarcan **todas las filas cargadas**, no toda la cuenta. Refresh history vuelve a la primera página. Las respuestas de recuperación actualizan filas ya cargadas, sin insertar filas sueltas de páginas aún no recorridas.

Export TXT exige resultado `completed/failed`, `synced_at` y transcripción. No exporta IDs del proveedor ni texto provisional. Inicio/fin y mute se conservan; End voice test dispone de bloqueo separado para no quedar bloqueado por consultas de historial.

**Real:** código de producción y rutas implementadas, compilación y navegador de esta revisión. **Fixtures:** 65 sesiones en pruebas locales, no representan actividad de NBC; Auth/DB/proveedor simulados a nivel HTTP. En tests de rutas se ejecutan `requireOperator`, las rutas, Supabase SDK, el adaptador ElevenLabs y servicios reales; no se reemplazó la autorización por un mock exitoso. **Pendiente:** ensayo remoto autenticado sobre sesión existente e integración del orquestador; evaluación humana de audio/mute. No se escribieron fixtures en Supabase ni se iniciaron llamadas.

## Archivos

| Archivo propio | Cambio |
|---|---|
| `src/components/dashboard/Caller.tsx` | Páginas, recuperación, estado parcial, selección y bloqueos de peticiones |
| `src/components/dashboard/Caller.module.css` | Controles de recuperación/paginación, foco y adaptación móvil con tokens NBC |
| `src/lib/caller-pagination.ts` (nuevo) | Cursor base64url versionado, fecha UTC con microsegundos, límites y frontera PostgREST |
| `src/lib/caller-types.ts` | Tipos de página y resultados de recuperación |
| `src/lib/caller-insights.ts` | Filtros sobre todas las páginas, fusión por ID, conservación de estados/texto/metadatos |
| `src/services/caller.service.ts` | Lecturas paginadas, recuperación acotada y guardado condicional; creación idempotente conservada |
| `src/app/api/caller/sessions/route.ts` | GET compatible con parámetros y `nextCursor`; POST de inicio conservado |
| `src/app/api/caller/sessions/reconcile/route.ts` (nuevo) | Auth real, cuerpo acotado/validado y respuesta por sesión |
| `tests/caller-pagination.test.ts` (nuevo) | Límites, cursores, fechas inválidas y precisión |
| `tests/caller-routes.test.ts` (nuevo) | Rutas/auth reales con transporte aislado; privacidad, páginas, recuperación y carreras |
| `tests/caller-test-loader.mjs` (nuevo) | Resolución de aliases/transpilación TS solo para tests; no sustituye auth ni servicios |
| `tests/caller-insights.test.ts` | Regresiones de finales, texto parcial, métricas/filtros y mezcla de formatos UTC |
| `tests/caller-continuity.spec.ts` (nuevo) | 65 fixtures, lotes, fallo/reintento, doble clic, selección, capturas desktop/mobile |
| `tests/caller-workspace.spec.ts` | Compatibilidad con recuperación al login y alcance de filtros; export verificado |
| `docs/features/caller-live-tests.md` | Diseño previo al código, contratos, resultado y límites de C01 |
| `docs/lanes/caller.md`, `docs/lanes/reports/C01.md` | Handoff y entrega revisable |
| `artifacts/lanes/C01/` | Logs, configuración de staging, capturas sintéticas y manifiesto |

Sin escritura fuera del ownership. Sin cambios en `integration.service.ts`, `elevenlabs.service.ts`, `.env*`, dependencias, shell o configuración global. `/sessions/:id/sync` conserva su archivo y ejerce el servicio actualizado. Las migraciones existentes no se tocaron.

## Evidencia y autorrevisión

Todos los comandos siguientes se ejecutaron el 2026-09-14. Logs finales en `artifacts/lanes/C01/`.

| Comando / comprobación | Entorno | Resultado real | Evidencia / pendiente |
|---|---|---|---|
| `node --experimental-strip-types --test tests/caller-*.test.ts` | Workspace, HTTP reemplazado por fixtures sin red externa | **15 pasan**, 0 fallos | `unit-tests.log`; incluye autorización real, aislamiento UUID, fechas iguales y 65 filas en tres páginas |
| `node node_modules/typescript/bin/tsc --noEmit --incremental false` | Workspace, sin artefactos compartidos | **Exit 0** | `typecheck.log` vacío por éxito |
| `NEXT_PUBLIC_SUPABASE_URL=https://caller-fixture.supabase.co NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=fixture-public node node_modules/next/dist/bin/next build --webpack` | Copia aislada, valores públicos sintéticos, sin `.env` | **Exit 0** | `build.log`; compila nueva ruta y todas las páginas del snapshot |
| `node node_modules/@playwright/test/cli.js test --config caller-playwright.config.mjs` | Copia aislada, Chrome, puerto propio 60382 | **2 pasan** | `browser-tests.log`; nuevo recorrido y regresión TXT/móvil |
| HTTP GET sessions / POST sync / POST reconcile sin Bearer | Servidor Next compilado aislado, sin interceptar las rutas | **401 y no-store en las tres** | `http-anonymous.log` |
| Inspección visual desktop 1440×1080 y móvil 390×844 | Browser real con fixtures explícitos | Capturas completas, móvil sin overflow horizontal | `desktop-fixtures.png`, `mobile-fixtures.png` |
| Comparación SHA256 del runtime propio con staging construido | Local | Coinciden los archivos propios probados | `candidate-check.log`, manifiesto |
| Conversación con micrófono / mute humano | No ejecutada | **PENDIENTE**, no autorizar consumo por tener una clave | Futuro ensayo autorizado; ningún audio nuevo en C01 |
| Recuperación remota autenticada Supabase/ElevenLabs | No ejecutada | **PENDIENTE de integración** | No se presenta el transporte fixture como validación cloud |

Fallos realmente observados y resueltos antes de esta entrega:
- Primera ejecución Node: 13/14 pasaron; una prueba histórica usaba igualdad de referencia al recuperar de `expired`. Se ajustó a igualdad de contenido porque la fusión ahora preserva metadatos; últimas dos ejecuciones: 15/15.
- Primer arranque aislado en puerto 3101 falló `EADDRINUSE`. El ensayo de navegador iniciado sobre ese puerto se interrumpió y no cuenta como validación. No se detuvo el proceso que ya ocupaba el puerto. Se eligió 60382, libre, y se verificó el arranque antes de reintentar. Logs `server-port-conflict.log` y `browser-tests-port-conflict.log`.
- Primer E2E contra el servidor propio: 1/2 pasó; selector genérico `role=alert` coincidía también con el anunciador de Next.js. Se acotó al mensaje del fixture; `browser-tests-selector-failure.log`. Dos ejecuciones posteriores: 2/2.
- Preparación inicial de la copia asumía `public/`, que no existe en este proyecto; se corrigió para copiar solo directorios existentes. No cambió la aplicación.

Autorrevisión: validación antes de interpolar cursor, filtros UUID en SELECT/UPDATE, campos privados fuera de respuesta, máximo cinco lecturas simultáneas, ausencia de POST/creación de voz en reconcile, finales inmutables, `expired` solo progresa a resultado final, marcas `synced_at` estrictamente crecientes para compare-and-set incluso dentro del mismo milisegundo. Hasta tres intentos locales de persistencia; no repite lecturas del proveedor por carrera. Transcripciones con menos turnos o menos texto no sustituyen las ya guardadas. Pruebas y export no contienen secretos reales.

No se ejecutaron builds/E2E ni reinicios sobre el servidor compartido 3000. El build usa `--webpack` para admitir `node_modules` instalado mediante enlace de solo uso en staging; no se cambió la configuración de build del proyecto. No se instalaron paquetes. El servidor aislado se detiene al entregar.

## Cómo lo revisa Franco

1. Tras integración local del orquestador, abrir `http://127.0.0.1:3000/caller` (o su URL local coordinada) e ingresar con el operador existente. No iniciar voz. Observar un único estado «Checking up to five saved sessions…» seguido del resumen por estado; si no hay pendientes, aparece el estado vacío.
2. Pulsar **Recover pending**: revisa hasta cinco reservas propias, incluidas expiradas. Si aparece **Recover next batch**, avanza hacia antiguas sin bloquearse por una que aún procesa. Puede repetirse Recover pending para reiniciar desde recientes. Fallos por sesión dejan disponible el resto y muestran IDs para buscarlos/reintentar con Refresh result.
3. En historial, pulsar **Load more** hasta el final. No deben repetirse filas; Refresh history vuelve a las treinta más recientes. Una llamada iniciada desde otro navegador no desplaza la continuación actual; aparecerá al refrescar. Búsqueda/estado aplican a todo lo cargado; métricas mantienen treinta recientes.
4. Seleccionar un resultado final y **Download transcript**: TXT guardado con texto y sin ID de proveedor. Una reserva pendiente no permite exportar. Durante una consulta de un pendiente, seleccionar otra sesión: la respuesta anterior no debe sustituir la selección nueva.
5. Si hay pocos datos reales, usar el escenario fixture siguiente; **no crear 65 sesiones ni llamadas en NBC**. Las capturas de esta entrega son fixtures; no prueban actividad real, audio ni calidad del agente.

### Simulación segura y reproducción aislada

`tests/caller-continuity.spec.ts` genera 65 filas `C01 DEMO fixture`, pone la 65 en `processing`, simula un lote con error individual y resuelve `/sync` solo después de seleccionar otra fila. También fuerza 503 en la primera página adicional, prueba doble clic y reintenta. `tests/caller-routes.test.ts` prueba con servicios reales un lote mixto (completed, processing, 404 temporal, 404 expirado y error 500). No necesita clave real ni cambios en SQL.

Copia utilizada: `/var/folders/hc/c9z9c365625f43ctpt9tpbgr0000gn/T/nbc-c01-7idal66y`; metadatos en `artifacts/lanes/C01/staging.json`. Es temporal, no un despliegue. Configuración de Playwright reproducible guardada en `artifacts/lanes/C01/caller-playwright.config.mjs` (copiar a la raíz de esa copia si se reconstruye). Contiene únicamente URL local, Chrome y nombres de pruebas.

Desde esa copia, con puerto libre y artefactos propios:

```sh
node node_modules/next/dist/bin/next start --hostname 127.0.0.1 --port 60382
```

En otra terminal, desde la misma copia:

```sh
node node_modules/@playwright/test/cli.js test --config caller-playwright.config.mjs
```

Para observar paso a paso los fixtures, agregar `--headed --debug tests/caller-continuity.spec.ts`. El operador y password del test son falsos e interceptados; no habilitan un login manual en Supabase. Si el puerto se ocupó, cambiarlo **solo en la configuración de la copia** y en `next start`. Nunca reiniciar otro servidor ni copiar secretos para ejecutar estos fixtures.

Capturas: `artifacts/lanes/C01/desktop-fixtures.png` y `mobile-fixtures.png`. La estética del shell corresponde al snapshot compartido de ese momento y no constituye revisión del lane I01; C01 modifica solo su workspace.

## Datos, APIs y configuración

**Esquema/migraciones:** ninguna migración nueva propuesta, probada o aplicada. Se usa `call_sessions`, FK operador/Auth, RLS y revocaciones existentes sin cambios. Índice existente `(operator_id,created_at desc)`; orden adicional por ID para estabilidad. Si crece el volumen, medir si requiere índice `(operator_id,created_at desc,id desc)`; no se propone aplicar DDL sin medición/revisión. No cambió retención.

**Auth común, sin cambios:** Bearer validado mediante Supabase `getUser`, email confirmado y permitido. Se usa el UUID obtenido en cada query. Sin Bearer: 401. Token rechazado/email incorrecto/no confirmado: 403 conforme al helper real actual. No se confía en IDs/claims del cliente. Respuestas `Cache-Control: no-store`.

| Endpoint | Request | Respuesta / errores |
|---|---|---|
| GET `/api/caller/sessions` | `limit` opcional entero decimal 1–30, default 30; `cursor` opcional. Parámetros repetidos cursor/limit rechazados | 200 `{sessions: CallSession[], configured: boolean, nextCursor: string|null}`. 400 cursor/límite inválido; 401/403 auth; 503 almacenamiento/configuración |
| POST `/api/caller/sessions/reconcile` | JSON `{}` o `{limit: 1..5, cursor?: string}`, default 5. Solo esos campos, límite numérico; JSON ≤8 KiB | 200 `{results: [{sessionId, outcome, session?, error?}], nextCursor}`; outcome `completed/failed/expired/pending/retry`. 400 formato/cursor/límite, 401/403 auth, 413 tamaño, 415 content type, 503 lectura inicial/DB. Fallo individual devuelve `retry` saneado dentro del 200, sin detener lote |
| POST `/api/caller/sessions/:id/sync` | UUID v4, Bearer; cuerpo no obligatorio, sin IDs externos | 200 `{session}`. 404 local para sesión ajena/inexistente; 404 externo temporal ahora mantiene sesión pendiente, tras diez minutos marca expired. 400 UUID, 401/403 auth, 502 proveedor/identidad no verificable, 503 almacenamiento/concurrencia |
| POST `/api/caller/sessions` | Contrato anterior `{sessionId: UUID v4}` | Sin cambios: 201 autorización; 409 duplicado/concurrencia, 429 cuota; no invocado por recuperación |

Cursor: base64url JSON `{v:1,createdAt:"fecha UTC Z o +00:00 con hasta 6 decimales",id:"UUID v4"}`, longitud máxima 512. Fecha válida, versión y UUID comprobados; sin texto arbitrario en el filtro. Frontera descendente estricta: `created_at < fecha OR (created_at = fecha AND id < uuid)`. No es un token de autorización; manipularlo solo mueve el punto de lectura **dentro de las filas propias**. La precisión original se conserva; no se convierte a Date al construir la frontera.

Sync nunca crea conversación ni acepta transcripción/duración cliente. Usa proveedor/agente/conversation ID guardados. Sin conversation ID, conserva la reserva antes de diez minutos y la expira después. Expiración es estado local de reserva, no prueba de ausencia definitiva del resultado. Puede recuperar `completed/failed` posteriormente; una respuesta provisional no la regresa a activa. Sin cambio en políticas SQL/permisos.

Variables existentes, sin valores reales ni altas nuevas: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`, `NBC_OPERATOR_EMAIL`, `ELEVENLABS_API_KEY`, `ELEVENLABS_AGENT_ID`. Ningún token administrativo es necesario. Operaciones cloud de C01: **ninguna**; no se consultó facturación ni se produjo consumo de voz.

## Documentación AInnovate

Feature y handoff actualizados antes/después de implementar. Se leyeron método completo, reglas, docs globales requeridas, ownership, tarea y protocolo. No se aplicó ninguna skill externa: la tarea usa servicios locales existentes y guías instaladas de Next.js. No se reinició Fase 1.

### Entrada propuesta para CHANGELOG

**[2026-09-14 16:22 EDT] — C01: continuidad de resultados e historial paginado**

Tipo: **ADDED / CHANGED / FIXED / SECURITY**.

Archivos: Caller.tsx/CSS; caller-types.ts, caller-insights.ts, nuevo caller-pagination.ts; caller.service.ts; GET sessions y nueva ruta reconcile; nuevos tests caller-pagination/caller-routes/caller-test-loader/caller-continuity; tests caller-insights/caller-workspace; feature/handoff/reporte C01 y evidencia propia.

Descripción: paginación compatible por fecha/UUID con precisión Postgres y límites validados. Recuperación autenticada de hasta cinco conversaciones existentes por lote al entrar o por acción explícita, continuación por cursor y errores individuales seguros. Guardado condicional preserva finales y texto verificado ante resultados parciales/concurrentes. UI carga más, evita duplicación/selección tardía y declara alcances de métricas/filtros. 15 pruebas Node, TypeScript, build aislado, 2 E2E fixture y 3 rechazos HTTP anónimos pasan. Sin migraciones, llamadas nuevas, cambios de proveedor ni publicación. Integración remota y evaluación humana pendientes.

Request original: «Tu tarea es extender ese recorrido para que salir de la página no deje resultados irrecuperables y el historial no se limite a treinta sesiones. No reconstruyas el módulo.»

### Deltas exactos pendientes de consolidar

- **DB_SCHEMA:** reemplazar la descripción de historial limitado y expiración solo al iniciar por: «Lecturas propias paginadas en orden `(created_at desc,id desc)`. Inicio conserva limpieza tras diez minutos; sync/reconcile también expiran reservas sin ID o no disponibles después de ese plazo. Expired solo puede avanzar a completed/failed. Escritura condicional por estado y synced_at con marca estrictamente creciente protege resultados concurrentes. Sin nuevas tablas, columnas, índices, RLS, funciones o migraciones». Documentar `synced_at` como última persistencia de sincronización, incluida comprobación de expiración; no equivale por sí sola a transcripción final exportable.
- **API_DOCS:** sustituir fila GET limitada a treinta por el contrato GET paginado de la tabla anterior; añadir POST reconcile con JSON, resultado por sesión y códigos completos. Actualizar sync: 404 de conversación temporal se convierte en 200 con estado guardado/expired; 404 local de propietario incorrecto sigue intacto. Conservar POST inicio y auth común sin cambios. Todos los contratos completos están en «Datos, APIs y configuración».
- **Arquitectura:** añadir `caller-pagination.ts`, tipos SessionPage/ReconcilePage, endpoint reconcile, loader de tests y nuevos tests caller-*. Flujo: login → primera página → un lote acotado; acciones posteriores por cursor; no scheduler. Métricas de treinta cargadas, filtros de todas las cargadas. Persistencia compare-and-set con tres intentos y lecturas externas acotadas. No interpretar build de snapshot compartido como aceptación de otras entregas.
- **Lookup en CLAUDE/.windsurfrules y equivalentes a cargo del orquestador:** `src/lib/caller-pagination.ts`, `src/app/api/caller/sessions/reconcile/route.ts`, `tests/caller-*` → `docs/features/caller-live-tests.md`, `docs/API_DOCS.md`, `docs/03-security.md`; recuperar referencia C01.
- **03-security:** sustituir «volver y refrescar» por «al volver y autenticar se consulta un lote de hasta cinco sesiones propias; se puede continuar/reintentar explícitamente; no existe worker». Añadir validación de cursor y filtro UUID permanente; recuperación solo lee conversaciones ya vinculadas, sin autorización de voz nueva. Sin cambio de auth/RLS ni política de retención.
- **Overview / decisiones:** C01 implementado localmente, pendiente de revisión/integración; no afirmar despliegue, ensayo cloud nuevo ni calidad humana.
- **CHANGELOG:** consolidar el bloque anterior. **Ninguno de estos cambios globales se marca completado** desde el lane.

## Criterios de aceptación y dependencias

| Criterio | Estado | Evidencia / límite |
|---|---|---|
| Documentar flujo/contratos antes del código | Cumplido | Sección diseño previo en feature |
| Cursor fecha+ID, límites/propietario, compatibilidad | Cumplido local | Tests reales de rutas, 65 fixtures, 3 páginas, fechas iguales/microsegundos/inserción |
| Hasta cinco recuperaciones, sin crear voz | Cumplido local | Adaptador real sobre transporte que rechaza creación; lote parcial y cursor siguiente |
| 404 temporal, expiración, errores parciales | Cumplido local | Tests de sync/reconcile y errores UI recuperables |
| No perder verificados ni retroceder finales | Cumplido local | CAS, carrera con completed, failed inmutable, expired recuperable, textos vacíos/menores preservados |
| Cargar más, métricas/filtros explícitos, selección | Cumplido local | E2E 65 filas y cambio de selección antes de resolver sync |
| Export verificado, móvil, controles conservados | Cumplido local / audio pendiente | TXT probado, 390 px sin overflow, mute/voz no ensayados con audio nuevo |
| Anónimo/operador incorrecto usando auth real | Cumplido local | getUser + requireOperator reales con respuestas HTTP fixture; 3 rechazos HTTP reales sin mocks |
| Integración remota sobre sesión real existente | Pendiente | Orquestador coordina validación sin crear voz nueva |
| Consolidación global, revisión técnica y Franco | Pendiente | Orquestador y Franco, según protocolo |

Riesgos concretos: no hay rate limiting general de sync/reconcile (el lote limita cada petición, no todas las pestañas/clientes); grandes historiales podrían necesitar índice compuesto tras medición. El cursor no fija una instantánea transaccional de toda la cuenta: permite avance estable sobre filas existentes y refresco explícito para nuevas. Retención remota puede impedir recuperar resultados antiguos; expired conserva texto local y posibilidad de consulta. El login sigue en memoria y requiere volver a autenticar al salir/recargar. Los tests de transporte no acreditan RLS actual ni credenciales externas; no se debilitaron esos controles.

## Estado de revisión y publicación

- Autorrevisión del lane: **COMPLETADA** para C01-R1 con evidencia local descrita.
- Revisión técnica del orquestador: **PENDIENTE**.
- Revisión de Franco: **PENDIENTE** para el manifiesto C01-R1; no existe aprobación previa de este candidato.
- Push: **NO EJECUTADO**.
- Deploy: **NO EJECUTADO**.
- Candidato propuesto: archivos propios listados en `artifacts/lanes/C01/manifest.sha256`, para integración/revisión local. Destino remoto no solicitado ni seleccionado.

La aprobación previa al push/publicación la exige la instrucción explícita de Franco («Antes de pushear cualquier cosa, yo reviso y ahí pushean») y `docs/lanes/REVIEW-PROTOCOL.md`. El candidato está preparado para su revisión; pasar pruebas no sustituye esa aprobación ni habilita desplegarlo por CLI/API.

## Correcciones tras revisión

Sin revisión externa recibida. Esta revisión 1 incluye las correcciones de autorrevisión y pruebas detalladas arriba. Si se modifica el candidato tras revisión, actualizar número, manifiesto, evidencia y aprobaciones; esta entrega no cubre cambios posteriores.

## Próxima tarea sugerida

Validar en entorno integrado la recuperación de una sesión de prueba **ya existente**, reingresar al módulo y comprobar su persistencia/exportación sin iniciar otra conversación. Requiere asignación del orquestador; no iniciada desde C01.
