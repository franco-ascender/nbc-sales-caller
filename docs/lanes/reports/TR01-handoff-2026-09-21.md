# TR01 — handoff a otro lane (2026-09-21)

Franco decidió pasar el Master Tracker a otro chat/lane por insatisfacción con el resultado visual entregado hasta acá. Este documento es la base del prompt de arranque para ese chat nuevo. No repite el reporte completo (`docs/lanes/reports/TR01.md`, `docs/features/master-tracker.md`), lo resume para no reingestar todo.

## Qué existe hoy en el código

- `/tracker` (nav "Master Dashboard"): 5 tabs — Overview, Ecosystem Health, Closer Performance, Meta Ads Health, Funnel % — con **datos DEMO explícitos**, sin conexión real.
- `src/components/tracker/MasterTracker.tsx` + `.module.css`: todo el layout de UI.
- `src/lib/tracker-demo.ts`: fixtures demo.
- `src/lib/tracker-charts.ts` (+ tests): helpers puros de geometría SVG.
- `src/lib/tracker-types.ts` (+ tests): contrato `TrackerSnapshot` genérico por proveedor.
- `src/services/tracker-ghl.service.ts`: conector real a GHL (`/opportunities/search`, `/calendars/events`), reutiliza credencial única de servidor. **Nunca se ejecutó contra la API real** — falta `GHL_PRIVATE_INTEGRATION_TOKEN`.
- `src/services/tracker-meta.service.ts`, `tracker-stripe.service.ts`: stubs, mismo contrato, devuelven 503 hasta tener credenciales.
- `src/services/tracker-connections.ts` + `/api/tracker/connections`: estado de conexiones, no usado por UI actualmente.
- Cards de Meta Ads / Stripe agregadas a `src/components/dashboard/Integrations.tsx` (conexiones, no el tracker en sí).
- Nav: se sacó "Mentor Chat" y se agregó "Master Dashboard" en `PlatformShell.tsx`.

## Por qué Franco lo rechazó

Feedback textual: "no se parece a Calyx" y "se ve feo / mal armado visualmente". Se corrigió un bug real (el gráfico de embudo colapsaba por escala lineal con valores de 5 órdenes de magnitud de diferencia) verificado con captura de pantalla real (Playwright, no solo lectura de código). Aun corregido, el rechazo se mantuvo ("sigue siendo una mierda"). **No hay una lista concreta de qué corregir** — Franco no señaló elementos puntuales, rechazó el conjunto. El lane nuevo necesita pedirle una referencia visual concreta (una captura marcada, o "quiero que se vea exactamente como esta pantalla") antes de iterar de nuevo a ciegas — iterar sin esa ancla ya se probó y no funcionó.

## Referencia de diseño que Franco dio (Loom + capturas de Calyx, 2026-09-17)

Producto de tracking para ofertas high-ticket. Transcript completo y resumen funcional están en `docs/features/master-tracker.md` sección "Referencia recibida: Calyx". Resumen: dashboard con tabs, widgets arrastrables/redimensionables (drag+resize), gráficos con curvas suaves, tabla de campañas Meta con ROAS, CRM con customer journey completo, Deal Analysis con lead scoring por IA y avatares por segmento, funnel real. El objetivo declarado de Franco: algo "20 veces mejor que Calyx".

## Estado de infraestructura y accesos (importante, evita repetir errores)

- **Única conexión con acceso real hoy: GoHighLevel.** Falta cargar `GHL_PRIVATE_INTEGRATION_TOKEN` en `.env.local` (Settings → Private Integrations en la location de GHL, scopes `calendars/events.readonly` y `opportunities.readonly`). `GHL_LOCATION_ID` ya está cargado.
- Meta Ads: falta crear la app (Marketing API). Stripe: falta acceso a la cuenta de revenue del cliente (distinta de `STRIPE_SECRET_KEY`, que es la cuenta de NBC Credits).
- **Bug de infraestructura ya corregido:** las variables de entorno no viajan solas de `.env.local` al deploy de Vercel — hay que sincronizarlas al entorno correcto con `node scripts/vercel-project.mjs` (ver `CLAUDE.md` vigente, sección "Publicación"). Ya se sincronizaron a Production: Supabase, ElevenLabs, GHL_LOCATION_ID/BASE_URL/VERSION. Falta agregar `GHL_PRIVATE_INTEGRATION_TOKEN` ahí también cuando Franco lo cargue.
- **Incidente ya resuelto:** un primer `--prod` real rompió el login porque el entorno Production de Vercel nunca había tenido las variables core — estaba corregido con una versión Preview aliaseada a mano. Ya está corregido y documentado en `docs/features/master-tracker.md`.
- Se detectó otro lane deployando a Production casi en simultáneo (agregó vars de Lead Engine) — coordinar publicaciones, no asumir que sos el único escritor del proyecto Vercel.
- El repo ahora tiene `.git` inicializado (antes no lo tenía) pero **sin commits todavía** — confirmar con el orquestador el flujo de commit antes de asumir nada sobre git.

## Ownership

Familia `docs/features/master-tracker.md` (o su reemplazo), `docs/features/tracker-*.md`, `src/components/tracker/`, `src/lib/tracker-*`, `src/services/tracker-*`. Conexiones del tracker se resuelven en este lane con Franco directamente (instrucción explícita suya del 2026-09-17), no se delegan a Infra por defecto.

## Recomendación concreta para el lane nuevo

No reconstruir todo de cero sin necesidad — el contrato de datos (`tracker-types.ts`), el conector GHL real y el inventario de métricas (`master-tracker.md`) son trabajo verificado y reutilizable; lo que falló fue el diseño visual de `MasterTracker.tsx`, no la arquitectura de datos. Antes de tocar código: pedirle a Franco una referencia visual exacta y puntual (no "hacé como Calyx" en general).
