# L02 — primera entrega local: acceso del scraper

2026-09-16. Implementación por orquestador; no publicada. **Entrega parcial de L02:** cierra L-AUTH; la búsqueda real sigue pendiente de acceso/proveedor y datos del piloto.

## Resultado

Las doce operaciones de planes/research exigen operador interno configurado y membresía existente admin/active. Suspensión, degradación y membresía ausente se rechazan antes de datos de negocio/proveedores; fallo de consulta falla cerrado. El diagnóstico de conexiones mantiene su guard admin. No se amplía acceso a otros usuarios ni se modifica SQL/ejecución.

I10/T1 resuelto en la misma integración: tsconfig excluye snapshots de artifacts, conservando src/tests; fixtures de portada entregan bytes ArrayBuffer compatibles con Request/Response. No se modifica validación runtime de JPEG ni se declara resuelto A2.

## Evidencia

- `node --experimental-strip-types --test tests/lead-engine-access.test.ts`: 11/11 (suite padre + 10 escenarios). Rutas, auth y SDK reales con transporte controlado; no demuestra DB/RLS o scraper remoto.
- `node --experimental-strip-types --test tests/lead-engine-*.test.ts`: 66/66.
- `npm test`: 188/188, cero fallos/skips.
- `node node_modules/typescript/bin/tsc --noEmit --incremental false`: exit0 después de T1. Log anterior conserva fallo histórico.
- Logs y hashes: `artifacts/orchestrator/L02-access/`. No build, browser, llamadas cloud, SQL, push ni deploy nuevos.

Archivos: helper lead-engine-auth; adaptadores lead-engine.service/lead-engine-scrape.service; tests/lead-engine-access; tsconfig y fixtures academy-covers. Documentación global consolidada por el orquestador.

## Revisión y pendientes

Revisar guard y prueba de permisos, ejecutar comandos anteriores. La entrega local no cambia todavía el portal Vercel. No hay fallback bootstrap para ejecutar research sin membresía: un admin debe tener su registro activo. Auth conserva restricción al email del operador para esas operaciones.

Próximo tramo L02: cuenta/token Apify, negocio/ciudad, Actor/build/precio y límite concreto; luego validar una búsqueda real y recuperar resultados/costo. No basta configurar token para activar ejecución. Discovery y verificación owner-cell siguen siendo hitos distintos. No hay nuevo presupuesto acordado. Revisión de Franco antes de push continúa pendiente.
