# Reporte TR01 — Master Dashboard/Tracker, entrega 1 + 2

Fecha y entorno: 2026-09-17, chat Claude nuevo (lane Master Tracker), sin acceso a producción/DB remota.
Estado de implementación: PARCIAL CON DEPENDENCIA. Entrega 1 (documental) completa; Entrega 2 agrega un conector real a GoHighLevel (código + pruebas unitarias), sin credenciales cargadas en este entorno ni ejecución contra la API real.
Lane y herramienta: Master Dashboard/Tracker — Claude Code.
Modelo efectivo y esfuerzo: Sonnet, esfuerzo medio (asignado por Franco en `docs/lanes/prompts/MASTER-TRACKER-CLAUDE.md`). Se mantuvo Sonnet/medio para el conector GHL porque reutiliza el patrón de credencial única ya existente (`integration.service.ts`), sin aislamiento multi-tenant nuevo. No se pidió cambio de modelo en esta entrega.
Cambios de modelo solicitados/confirmados por fase y modelo al que volver: ninguno ejecutado. Pendiente: cuando haya más de una cuenta GHL/Meta/Stripe real (multi-cliente), pedir Opus/alto para el diseño de almacenamiento de credenciales por cliente, y volver a Sonnet para la vista/UI.
Modalidad de facturación y fuente de uso: desconocida (no medida en este chat).
Costo/tokens reportados, intentos y retrabajo: desconocido; no se completó `docs/lanes/TASK-COST-TEMPLATE.json` con cifras porque no hay medición disponible en este chat. Cero reintentos fallidos.
Objetivo asignado: TR01 — cerrar definición del primer recorrido de tracking e inventariar datos reales mientras llega la referencia de Anas (`docs/lanes/tasks/OR04-start-2026-09-17.md`).
Candidato: sin commit (repo local sin Git inicializado según entorno); archivos nuevos listados abajo.

## Resultado

**Entrega 1 (documental):** contrato de definición por cifra e inventario de datos reales existentes. Se identificaron las fuentes reales disponibles (`call_sessions`, `usage.ts`/`AdminUsage`, créditos/wallet, Lead Engine) y las que no existen (Ads, ingresos/ROAS, canal telefónico en producción, integración GHL/Calendar externo), evitando tratarlas como cero.

**Entrega 2 (código, tras recibir referencia Calyx y decisión de arrancar por GHL):** Franco compartió screenshots + transcript de Calyx (referencia de diseño) y confirmó que la única cuenta con acceso real hoy es GoHighLevel, con el objetivo de dejar la infraestructura de conectores lista para que sumar Meta/Stripe después sea "cambiar la API", no rediseñar. Se implementó:

- Contrato de snapshot genérico por proveedor (`TrackerSnapshot`), agnóstico de fuente.
- Mappers puros de datos GHL (citas por estado, pipeline abierto/ganado con valor en cents), que tratan datos faltantes como `null`, nunca `0`, y descartan registros de otra location.
- Servicio real `tracker-ghl.service.ts` que llama `POST /opportunities/search` y `GET /calendars/events` de GHL, reutilizando la credencial de servidor única que ya existía para el webhook/contact-check (`GHL_PRIVATE_INTEGRATION_TOKEN`/`GHL_LOCATION_ID`). No se creó almacenamiento de secretos nuevo.
- Stubs `tracker-meta.service.ts`/`tracker-stripe.service.ts` con el mismo contrato, hoy devuelven "no configurado" (503) porque no hay credenciales — es el punto exacto donde se completa cuando llegue el acceso.
- Agregador `tracker-connections.ts` para un futuro estado de conexiones (not_configured/configured), vocabulario compartido con I10 para no duplicar.

**Límite explícito:** los nombres de campo de la respuesta GHL (`monetaryValue`, `status`, `appointmentStatus`) están tomados de la documentación pública de la API v2, no verificados contra una respuesta real de esta cuenta — no se ejecutó ninguna llamada real (no hay credenciales cargadas en este entorno). No hay UI/widget que consuma esto todavía; no hay multi-cliente (una sola location GHL, la de Franco).

## Archivos

- `docs/features/master-tracker.md`: definición TR01, referencia Calyx resumida, decisión de arrancar por GHL, arquitectura de conectores.
- `docs/lanes/reports/TR01.md` (este archivo).
- `src/lib/tracker-types.ts` (nuevo): tipos y mappers puros del snapshot de tracker.
- `src/services/tracker-ghl.service.ts` (nuevo): conector real GHL (opportunities + calendar events).
- `src/services/tracker-meta.service.ts`, `src/services/tracker-stripe.service.ts` (nuevos): stubs con el mismo contrato, 503 hasta tener credenciales.
- `src/services/tracker-connections.ts` (nuevo): estado agregado de los tres conectores.
- `tests/tracker-ghl.test.ts` (nuevo): 5 pruebas unitarias de los mappers, sin red.
- `.env.example`: agregadas `META_ACCESS_TOKEN`, `META_AD_ACCOUNT_ID`, `TRACKER_STRIPE_SECRET_KEY`.
- `src/app/(workspace)/tracker/page.tsx`, `src/components/tracker/MasterTracker.tsx`, `MasterTracker.module.css` (nuevos): tab "Master Dashboard", admin-only, muestra estado de conexiones (sin secretos ni llamada real a proveedores).
- `src/app/api/tracker/connections/route.ts` (nuevo): GET protegido por `requireWorkspaceAdmin`, expone `trackerConnectionStatus()`.
- `src/components/platform/PlatformShell.tsx` (fuera de ownership original de este lane, editado por instrucción directa de Franco): una línea de navegación nueva (`/tracker`, ícono `Gauge`) + import de ícono. Señalar a Infra/orquestador para evitar choque con otro cambio de shell en curso.

No se tocó `integration.service.ts` ni la config GHL existente; se reutilizan sus mismas variables de entorno.

## Evidencia y autorrevisión

| Comando / comprobación | Entorno y fecha | Resultado real | Evidencia / pendiente |
|---|---|---|---|
| Lectura `docs/DB_SCHEMA.md`, `CallerAnalytics.tsx`, `usage.ts` | Local, 2026-09-17 | Confirmado inventario Entrega 1 | Citado en master-tracker.md |
| Lectura `src/services/integration.service.ts`, `lead-engine-connections.ts`, `.env.example` | Local, 2026-09-17 | Confirmado patrón existente: credencial única de servidor, sin multi-tenant, validación defensiva con `isRecord` | Reutilizado en `tracker-ghl.service.ts` |
| WebFetch/WebSearch documentación pública GHL API v2 (`marketplace.gohighlevel.com/docs`) | Web, 2026-09-17 | Confirmado: `/opportunities/search` (POST) y `/calendars/events` (GET) existen, soportan Private Integration token, header `Version: v3` | Nombres de campo del payload (`monetaryValue`, `appointmentStatus`) no confirmados contra respuesta real de esta cuenta |
| `npx tsc --noEmit -p .` | Local, 2026-09-17 | Sin errores | — |
| `npm run build` | Local, 2026-09-17 | Build de producción exitoso; `/tracker` y `/api/tracker/connections` aparecen en el listado de rutas | — |
| `npm test` | Local, 2026-09-17 | 207/207 pasan (202 previas + 5 nuevas) | Solo pruebas puras/fixtures; ninguna golpea GHL real |

No se ejecutó ninguna llamada real a GHL: no hay `GHL_PRIVATE_INTEGRATION_TOKEN` cargado en este entorno. Distinguir explícitamente: mappers = probados y reales; llamada HTTP a GHL = implementada pero no ejercida contra la API real todavía.

## Cómo lo revisa Franco

1. Leer `docs/features/master-tracker.md` completo (contrato + arquitectura de conectores).
2. Cuando quieras probar contra GHL real: cargar `GHL_PRIVATE_INTEGRATION_TOKEN`/`GHL_LOCATION_ID`/`GHL_CALENDAR_ID` en `.env.local` (ya existían para el webhook) y pedir una corrida de `fetchGhlSnapshot` contra un rango conocido, comparando contra lo que ves en el panel de GHL.
3. Antes de esa prueba, revisar conmigo los nombres de campo de la respuesta real (marcado como pendiente de verificación en el documento) — si difieren, se corrige solo el mapper, no la arquitectura.
4. No hay UI ni capturas todavía: esto es capa de servicio, sin pantalla.

## Datos, APIs y configuración

Sin migraciones nuevas. Endpoints externos usados (no propios): GHL `POST https://services.leadconnectorhq.com/opportunities/search` y `GET /calendars/events`, ambos con `Authorization: Bearer <token>` y `Version: v3`, mismas credenciales que `integration.service.ts`. Variables nuevas (solo nombres, sin valores): `META_ACCESS_TOKEN`, `META_AD_ACCOUNT_ID`, `TRACKER_STRIPE_SECRET_KEY`. Ninguna tiene valor cargado; los conectores Meta/Stripe devuelven 503 hasta que existan.

## Documentación AInnovate

Feature actualizado (`master-tracker.md`) con la arquitectura de conectores.

### Entrada propuesta para CHANGELOG

2026-09-17 — ADDED — `docs/features/master-tracker.md`, `src/lib/tracker-types.ts`, `src/services/tracker-ghl.service.ts`, `src/services/tracker-meta.service.ts`, `src/services/tracker-stripe.service.ts`, `src/services/tracker-connections.ts`, `tests/tracker-ghl.test.ts`, `.env.example` — Contrato genérico de conectores del Master Tracker (TR01) con implementación real para GoHighLevel (opportunities/calendar events) reutilizando la credencial de servidor existente, y stubs Meta/Stripe con el mismo contrato para conectar sin rediseño cuando haya acceso. Request: "la única a la que tengo acceso hoy es GoHighLevel. Quiero avanzar lo máximo posible para yo después llamarlo a Naci [...] cosa de que sea literalmente cambiar la API y que funcione."

### Deltas exactos pendientes de consolidar

- DB_SCHEMA: no aplica (sin tablas nuevas).
- API_DOCS: agregar sección de conectores GHL/Meta/Stripe del tracker (endpoints externos consumidos, no propios) cuando el orquestador consolide.
- Arquitectura y lookup: agregar fila `master-tracker.md` a la tabla de lookup de CLAUDE.md.
- Otros docs globales: `docs/00-current-state.md` al actualizarse; lo hace el orquestador.

## Criterios de aceptación y dependencias

| Criterio de la tarea | Cumplido / pendiente | Evidencia o dependencia |
|---|---|---|
| Pedir referencia de Anas | Cumplido (pedido a Franco en el chat y en el documento) | Pendiente respuesta de Franco |
| Inventariar datos reales mientras llega | Cumplido | Tabla en `master-tracker.md` |
| Contrato por cifra (pregunta/fórmula/fuente/cliente/período/moneda/actualización/faltantes) | Cumplido para las cifras identificadas | Mismo documento |
| Implementación delimitada de un tracker | Parcial | Conector GHL real implementado y probado; falta ejecutarlo contra la cuenta real y construir UI |
| No recrear toda la plataforma / no pantallas finales | Cumplido | Sin componentes UI nuevos, solo capa de servicio |
| Infra de conectores reutilizable para Meta/Stripe sin rediseño | Cumplido | Contrato `TrackerSnapshot` + stubs con mismo contrato |

## Estado de revisión y publicación

- Autorrevisión del lane: hecha. `npx tsc --noEmit` y `npm test` (207/207) pasan.
- Revisión técnica del orquestador: PENDIENTE.
- Revisión de Franco: PENDIENTE.
- Push: NO EJECUTADO.
- Deploy: NO EJECUTADO.
- Candidato y destino: sin candidato de despliegue; archivos de documentación y código nuevo listados arriba, sin credenciales ni datos reales incluidos.

## Próxima tarea sugerida

1. Franco carga `GHL_PRIVATE_INTEGRATION_TOKEN`/`GHL_LOCATION_ID`/`GHL_CALENDAR_ID` en `.env.local` para correr `fetchGhlSnapshot` una vez contra datos reales y verificar los nombres de campo marcados como pendientes.
2. Con eso verificado, construir el primer widget de UI (`src/components/tracker/`) que consuma el snapshot — probablemente "citas" u "pipeline abierto/ganado", que ya tienen mapper real.
3. Cuando Franco consiga acceso a Meta o Stripe, completar el servicio correspondiente (mismo contrato) y avisar si esa fase justifica pedir Opus/alto (multi-cuenta, dinero).
4. Aún sin usar: referencia de Anas si llega a aportarse por separado; el inventario original (Entrega 1) sigue vigente para las cifras que no dependen de GHL.

---

Reporte guardado en docs/lanes/reports/TR01.md. Listo para revisión del orquestador y de Franco; push no ejecutado.

---

## Entrega 3 — centro operativo visual (2026-09-21)

**Estado:** candidato visual verificado localmente; publicación de esta revisión pendiente al momento de esta entrada.  
**Modelo y esfuerzo:** Terra / medio, apropiado para la implementación UI de alcance delimitado. No se activaron flujos de credenciales, permisos, SQL o proveedores.

### Qué se implementó

Se reemplazó la composición visual rechazada por un Overview de Tracker que toma como referencia funcional la claridad de Callix/Calyx, sin copiarla literalmente: una vista donde el dueño puede conectar caja, pipeline, llamadas, adquisición y calidad comercial con la siguiente acción.

- Encabezado con período visible, etiqueta inequívoca **Preview data** y acceso a personalizar la presentación.
- Tendencia de caja explorables con cursor; el valor cambia por día de la serie de muestra.
- Bloques de pulso de negocio, adquisición, desempeño comercial, fuentes y calidad de ventas organizados por problema operativo.
- Estados honestos: GoHighLevel pendiente de verificación, Meta no conectada, Stripe pendiente de mapeo y transcripciones de Caller aún no autorizadas para análisis. Ningún faltante se presenta como cero ni como insight real.
- Personalización local: ocultar/mostrar y reordenar secciones, restablecer orden, persistida sólo en el navegador bajo `nbc-tracker-layout-v1`. No modifica datos ni se sincroniza entre dispositivos.
- Vista móvil y modo oscuro verificados; no se agregaron dependencias, assets ni llamadas a proveedores.

### Real, demo y pendiente

| Área | Estado | Alcance comprobable |
|---|---|---|
| Interacción, personalización y gráfico | Real en la UI | Cursor sobre el gráfico, reordenar, ocultar, mostrar y resetear funcionan localmente. |
| Métricas monetarias, llamadas, adquisición y reps | Preview | Son valores de demostración explícitamente rotulados; no derivan de Stripe, GHL, Meta ni Caller. |
| GoHighLevel | Pendiente de validación | Conector de la entrega anterior permanece reutilizable, pero no se llamó a la API real porque falta el token de integración. |
| Meta, Stripe y transcripciones | No conectados / pendientes | No se inventan atribución, gasto, objeciones ni grabaciones. |

### Archivos de esta revisión

- `docs/features/master-tracker.md`: contrato de presentación, referencia visual, límites de personalización y aceptación.
- `src/components/tracker/MasterTracker.tsx`: Overview operativo e interacciones de personalización.
- `src/components/tracker/MasterTracker.module.css`: sistema visual responsive de la pantalla.
- `src/lib/tracker-workspace.ts`: contrato puro y defensivo para el layout local.
- `tests/tracker-workspace.test.ts`: persistencia corrupta, visibilidad y reordenamiento sin mutación.
- `tests/tracker-browser.mjs`: revisión browser con autenticación sintética, interacción y cortes responsive.
- `artifacts/lanes/TR01/r2/`: capturas y resultado de revisión del navegador.

### Candidato verificable

Sin commit: el repositorio local continúa sin commits base. Hashes SHA-256 del candidato revisado:

```
333cece002d6910cffa031bad008bc264907c11cba4a3ee9c972f8194e4d86b4  src/components/tracker/MasterTracker.tsx
863f584074c3e9d9924b1828442690249c6747ec02946cac0893bee12dd61025  src/components/tracker/MasterTracker.module.css
c88595fc2fdecc35e31d6089c36781be8a649e6f29185a754645d9829483a419  src/lib/tracker-workspace.ts
65695f02ca9292648adc48caf650d0955b020c11c451d5e5b860e7f146d27de6  tests/tracker-workspace.test.ts
d3c569a6f2ff3eb4df8b05805ea6662854b039e5b42cedd4f454f39ef2fc18f6  tests/tracker-browser.mjs
4cdd9c1079d290e38340c6d72c47bd3910c28d9f3ef40cb9ffb32ff28454c166  docs/features/master-tracker.md
```

### Pruebas ejecutadas

| Comando | Entorno / fecha | Resultado |
|---|---|---|
| `node node_modules/typescript/bin/tsc --noEmit --incremental false` | local, 2026-09-21 | Pasa. |
| `node --experimental-strip-types --test tests/tracker-charts.test.ts tests/tracker-ghl.test.ts tests/tracker-workspace.test.ts` | local, 2026-09-21 | 12/12 pasan. |
| `NEXT_PUBLIC_SUPABASE_URL=… NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=… node node_modules/next/dist/bin/next build --webpack` | copia aislada `/tmp/nbc-tracker-r2-bMiFaW`, 2026-09-21 | Pasa; ruta `/tracker` incluida. |
| `TRACKER_REVIEW_BASE_URL=http://127.0.0.1:3034 TRACKER_REVIEW_ARTIFACT_DIR=artifacts/lanes/TR01/r2 node tests/tracker-browser.mjs` | servidor aislado, 2026-09-21 | Pasa 4 comprobaciones; 0 errores de página, 0 solicitudes externas a proveedores. |

La primera corrida browser detectó que un selector buscaba el texto de Meta de forma demasiado estricta; se corrigió el selector y la corrida final anterior es la evidencia válida. No se corrieron llamadas reales a GHL, Meta, Stripe ni Vimeo.

### Evidencia visual

- `artifacts/lanes/TR01/r2/tracker-overview-desktop.png`
- `artifacts/lanes/TR01/r2/tracker-overview-mobile.png`
- `artifacts/lanes/TR01/r2/tracker-overview-mobile-dark.png`
- `artifacts/lanes/TR01/r2/browser-checks.json`

### Pasos de revisión

1. Abrir `/tracker` con una cuenta administradora.
2. Confirmar la etiqueta **Preview data**, el período y las razones de cada fuente pendiente.
3. Mover el cursor sobre la tendencia de caja para inspeccionar el valor diario de demostración.
4. Abrir **Customize**, ocultar `Business pulse`, subir `Sales performance`, y usar **Reset layout**; comprobar que no cambia ninguna métrica.
5. Abrir `Acquisition` y `Sales quality`: deben explicar `Meta not connected` y que las transcripciones de Caller aún no están disponibles, sin mostrar datos atribuidos falsamente.
6. Repetir en móvil; la página debe conservar lectura y no desbordar horizontalmente.

### AInnovate / consolidación pendiente

**CHANGELOG propuesto:** `2026-09-21 — CHANGED — docs/features/master-tracker.md, src/components/tracker/MasterTracker.tsx, src/components/tracker/MasterTracker.module.css, src/lib/tracker-workspace.ts, tests/tracker-workspace.test.ts, tests/tracker-browser.mjs — Reemplaza el layout de Tracker por un Overview operativo, personalizable y trazable, inspirado funcionalmente en Callix/Calyx; todos los indicadores sin fuente verificada quedan como preview o pendientes. Request: centralizar gasto, llamadas, facturación y objeciones en una superficie premium y útil.`

- `DB_SCHEMA`: sin cambios.
- `API_DOCS`: sin endpoints nuevos en esta revisión.
- Arquitectura / lookup global: el orquestador debe consolidar sólo si lo considera necesario; no se marca como hecho aquí.

### Riesgos, dependencias y siguiente tarea sugerida

- La UI no convierte los previews en datos reales. Para hacerlo se necesita `GHL_PRIVATE_INTEGRATION_TOKEN`, confirmación de campos contra una cuenta real, y accesos autorizados separados para Meta/Stripe/Caller.
- La personalización es local por diseño. Guardarla por usuario y compartir vistas requiere definir propiedad, permisos y persistencia antes de añadir una tabla.
- Próxima tarea sugerida, sin iniciar: ejecutar un snapshot GHL con token autorizado, confrontarlo con un período conocido y conectar los primeros widgets de pipeline/citas sin alterar el contrato genérico.

### Revisión y publicación

- Revisión técnica local: APROBADA por las pruebas enumeradas.
- Revisión de producto de Franco: PENDIENTE.
- Push: NO EJECUTADO.
- Estado de Vercel antes de publicar: producción actual `Ready`; verificado con `node scripts/vercel-project.mjs --check` el 2026-09-21.

### Publicación y comprobación final

- Publicación: `node scripts/vercel-project.mjs --deploy`, 2026-09-21. Producción `Ready`: `https://nbc-sales-mt5mfocpu-nbc-sales.vercel.app`, alias canónico `https://nbc-sales.vercel.app`.
- Build remoto: correcto; TypeScript correcto; `/tracker` generado en el listado de rutas.
- Smoke HTTP: `curl -I --max-time 20 https://nbc-sales.vercel.app/tracker` devolvió `HTTP/2 200`.
- Revisión browser contra el alias canónico: `TRACKER_REVIEW_BASE_URL=https://nbc-sales.vercel.app TRACKER_REVIEW_ARTIFACT_DIR=artifacts/lanes/TR01/r2-production node tests/tracker-browser.mjs` pasa 4/4 con sesión sintética, 0 errores de página, 0 llamadas a proveedores y 0 solicitudes externas no permitidas. Evidencia: `artifacts/lanes/TR01/r2-production/`.
- Push: **NO EJECUTADO**. La publicación en Vercel no es un push ni una aprobación de código; la revisión de producto de Franco sigue pendiente.

---

## Entrega 4 — estructura completa de Tracker (2026-09-21)

**Estado:** estructura de producto completa en revisión local; fuentes reales deliberadamente pendientes para la sesión de conexión.

El Overview ya no es la única superficie. Se construyeron las siete vistas que usarán los conectores existentes y futuros sin rediseñar el recorrido mañana:

| Vista | Estructura entregada | Datos actuales |
|---|---|---|
| Overview | señal de caja, pulso, adquisición, ventas, calidad y cobertura | preview explícito |
| Revenue & pipeline | métricas, tablero de etapas y actividad reciente | preview anónimo |
| Sales quality | camino booked→showed→offer→won, scorecard y coaching | preview; transcripts pendientes |
| Acquisition | campañas y evaluación de eficiencia | preview; Meta no conectado |
| Funnel | etapas, conversiones y menor handoff para investigar | preview |
| Accounts & journeys | cola de cuentas anónimas, detalle, timeline y siguiente acción | preview; CRM/GHL pendiente |
| Data sources | contrato por proveedor, campos esperados, estado y entrada hacia Integrations | real como documentación/UI; no conecta proveedores |

También se agregó selector de período para recorrer la UI de preview. No recalcula ni atribuye datos: conserva la etiqueta Preview data. Los registros añadidos son `Account A` a `Account D` y `Rep A/B`, sin identidades, cuentas, campañas ni transcripciones reales.

### Archivos de esta revisión

- `docs/features/master-tracker.md`: mapa de vistas, campos esperados, límites y criterios de la estructura completa.
- `src/components/tracker/MasterTracker.tsx`: navegación, vistas y estados de interacción.
- `src/components/tracker/MasterTracker.module.css`: superficies responsive de pipeline, calidad, funnel, journeys y fuentes.
- `src/lib/tracker-demo.ts`: fixtures anónimos de etapas, actividad y journeys sólo para revisar el diseño.
- `tests/tracker-browser.mjs`: amplía la verificación de navegador a las siete vistas.
- `artifacts/lanes/TR01/r3/`: capturas y comprobación de browser para este corte.

### Pruebas ejecutadas

| Comando | Entorno / fecha | Resultado |
|---|---|---|
| `node node_modules/typescript/bin/tsc --noEmit --incremental false` | local, 2026-09-21 | Pasa. |
| build aislado de Next con variables públicas fixture | `/tmp/nbc-tracker-r3-*`, 2026-09-21 | Pasa; `/tracker` generado. |
| `TRACKER_REVIEW_BASE_URL=http://127.0.0.1:3035 TRACKER_REVIEW_ARTIFACT_DIR=artifacts/lanes/TR01/r3 node tests/tracker-browser.mjs` | servidor aislado, 2026-09-21 | Pasa 6/6: Overview, personalización, estados de fuentes, revenue/pipeline, funnel, journeys y fuente; móvil claro/oscuro sin desborde; 0 errores y 0 solicitudes externas a proveedores. |

### Próxima sesión funcional

1. Verificar GHL con un token de Private Integration y un rango conocido; confirmar campos de opportunity/calendario antes de alimentar pipeline, agenda y journeys.
2. Definir una fuente de cash (Stripe u otra), qué significa "cash collected" y el handling de refunds/currency.
3. Conectar Meta con cuenta y ventana de atribución confirmadas para sustituir campañas y funnel de adquisición.
4. Acordar el permiso de transcript Caller, taxonomía de objeciones y retención antes de alimentar Sales quality.

No se aplicaron migraciones, no se almacenaron secretos y no se invocaron APIs externas durante esta entrega. Push continúa **NO EJECUTADO**.

### Publicación de Entrega 4

- Preflight: `node scripts/vercel-project.mjs --check` confirmó producción previa `Ready`.
- Publicación: `node scripts/vercel-project.mjs --deploy`, 2026-09-21. Producción `Ready`: `https://nbc-sales-2z6qxyvhw-nbc-sales.vercel.app`; alias canónico preservado: `https://nbc-sales.vercel.app`.
- Build de Vercel: compilación y TypeScript correctos; `/tracker` incluido como ruta estática.
- Revisión final de producción: `TRACKER_REVIEW_BASE_URL=https://nbc-sales.vercel.app TRACKER_REVIEW_ARTIFACT_DIR=artifacts/lanes/TR01/r3-production node tests/tracker-browser.mjs` pasó 6/6 con fixtures sintéticos, sin errores de página ni solicitudes a proveedores. `curl -I --max-time 20 https://nbc-sales.vercel.app/tracker` devolvió `HTTP/2 200`.
- Evidencia: `artifacts/lanes/TR01/r3-production/`.
