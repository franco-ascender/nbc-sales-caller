# Reporte I02 — revisión 4: Start Here y navegación de mentoría

## Corrección de acceso de Franco — 2026-09-14

Franco reportó el error de membresía en Start Here y pidió activar su acceso. Su cuenta Auth de operador existía y el login general la admitía por bootstrap, pero faltaba su fila en `nbc_members`. Se creó membresía **admin activa** para esa misma cuenta, conservando email/password. Verificados con su login real sobre Vercel: `/api/workspace/session`, `/api/members` y `/api/members/summary` retornan 200; navegador muestra las preguntas de Start Here. Evidencia saneada en `artifacts/lanes/I02/operator-access/`. Sin cambios de código/deploy, correos ni respuestas de onboarding inventadas. Las dos cuentas de Anas/Elias se conservan.

2026-09-14. **PUBLICADO Y VERIFICADO EN VERCEL** por instrucción de Franco: separar onboarding/roadmap de calendario, chat, soporte y créditos. Fuente congelada: `artifacts/lanes/I02/navigation/`, 109 archivos, digest de manifiesto normalizado `48094c981a03323444cc9f316605ba5873a0ba38d6f2249a39b641234fa01498`. Deployment `dpl_CvvZZYvjp8nx84xQqvujyk3mrJER` READY, URL inmutable https://nbc-sales-5ljr7c2nt-nbc-sales.vercel.app/, alias principal https://nbc-sales-nbc-sales.vercel.app/.

## Resultado observable

- El antiguo enlace Members se llama **Start Here**, con icono de huellas, antes de completar onboarding. `/members` conserva compatibilidad del enlace.
- Dos preguntas provisionales: qué está haciendo el miembro y qué quiere lograr. Guardado real de borrador y botón explícito de completar. Se identifica como formulario temporal; no se inventó el formulario definitivo de NBC.
- Tras completar, la pantalla y enlace cambian a **Your Roadmap**, con mapa. Muestra exactamente **Roadmap charging** y explica que aparecerá cuando esté preparada. Las respuestas de onboarding no se presentan como una roadmap; todavía no existe carga/generación de roadmaps.
- **Calendar** (`/calendar`) y **Mentor Chat** (`/mentor-chat`) son enlaces laterales independientes. Conservan las funciones y permisos existentes, sin tabs dentro de Members.
- **Support** (`/support`) es icono con nombre accesible junto a NBC workspace al pie del menú; queda visible en escritorio y dentro del menú móvil. Se retiró el bloque promocional repetido para reservar ese espacio. Navegación lateral puede desplazarse sin expulsar el pie.
- **NBC Credits** queda en el extremo derecho de cabecera, fija durante scroll. Saldo disponible real, enlace a `/credits` para historial/asignación. En móvil icono y número mantienen nombre accesible completo. Carga/error muestra “—”, nunca cero inventado.
- La revisión de miembros por admin/coach queda en un bloque secundario plegable. El saldo/estado del shell siempre pertenece a quien inició sesión, no al estudiante inspeccionado. Se conservan asignación de coach, soporte, eventos, mensajes y créditos manuales.

## Implementación y contratos

Catorce archivos runtime propios seleccionados: tipos/servicio Members, nuevo GET `/api/members/summary`, contexto `MemberNavigation`, dos componentes Members y CSS, shell/CSS, layout y cuatro páginas. Detalle en `staging.json.selectedFiles`. No nueva migración ni cambios a datos de Anas/Elias, permisos comerciales o consumo automático.

GET `/api/members/summary`: Bearer validado con `requireMember`, solo miembro activo. Retorna `{completed:boolean,available:number}` leyendo onboarding y wallet del actor. No acepta selección de otro miembro; no devuelve preguntas, historial ni PII. no-store; 401 anónimo/token inválido, 403 sin membresía activa, 503 DB, 500 inesperado saneado. RLS y servicio siguen intactos. El contexto carga después de login, actualiza por navegación/foco/guardado, descarta respuestas viejas y no reutiliza estado de otro token.

Se combinaron **snapshots publicados** C02-R1 y K01-R3, ambos derivados de I02-R3, comprobando hashes y ausencia de conflictos en cambios contra esa base. Se preservan Caller y Academy en el mismo deploy. No se publicó el workspace mutable ni se aplicó SQL pendiente de Academy. `staging.json.preservedPublishedChanges` enumera las fuentes. Build propio en `/private/tmp/nbc-navigation-uabexbcf`, puerto 3116; servidor compartido intacto.

## Evidencia

- `npm ci` y build Node24/Next16.3.5: aprobados, `build-final.log`. Primer build detectó colisión del icono Map con el constructor de colección; corregida importación a MapIcon antes del build final.
- `node scripts/deploy-member-navigation-check.mjs http://127.0.0.1:3116 local`: seis escenarios con múltiples aserciones reales, aprobados. Escritorio/móvil: borrador recuperado tras reload/login, completar cambia texto/icono, rutas laterales, soporte visible al pie, saldo 17 de prueba fijo al scroll, revisión admin mantiene su propio saldo, login en rutas nuevas y rechazo anónimo summary. Sin mocks, sin errores JS ni overflow. Dos cuentas temporales/datos eliminados después de cada ejecución; admins no alterados.
- Repetición HTTPS: **seis escenarios aprobados**, sin errores JS, datos reales y ninguna request interceptada. Dos cuentas temporales y sus datos eliminados. Evidencia/capturas: `vercel-checks.json`, `vercel-checks.log`, `vercel-desktop-start.png`, `vercel-desktop-roadmap.png`, `vercel-mobile-start.png`, `vercel-mobile-roadmap.png`. Identidad visible es TEST Member; no credenciales.
- Manifiesto, archivo reproducible/hash y escaneo de secretos: `runtime-manifest.json`, `runtime-archive.json`, `security.json`. Sin env, scripts, pruebas, SQL o documentos en upload. Se reutilizan variables preview. APIs y login siguen protegidos; excepción Vercel solo por deployment.

## Revisión y publicación

Autorrevisión completada; publicación de corrección dentro de autorización vigente de Franco. Push no ejecutado. No aprobación del orquestador inventada. Para revisar: entrar al portal normal, seleccionar Start Here, responder/completar y observar Your Roadmap; usar Calendar/Chat lateral, Support inferior y créditos superiores. Cuentas Anas/Elias conservan sus contraseñas. La roadmap definitiva y el formulario real quedan pendientes de contenido, como pidió Franco.

## Deltas documentales AInnovate

CHANGELOG propuesto: 2026-09-14, CHANGED navegación Members en I02-R4: Start Here → Your Roadmap, rutas laterales, soporte inferior y créditos globales. Request: «un Start Here [...] cuando ya hayas hecho el onboarding, que te lleve a tu roadmap [...] Roadmap charging [...] calendar [...] en el costado [...] Support [...] al lado de NBC Workspace [...] NBC Credits [...] arriba a la derecha». DB_SCHEMA sin cambios. API_DOCS agrega summary con contrato anterior. Arquitectura/lookup agrega MemberNavigation, cuatro páginas y ruta summary; la ruta Members se conserva como journey. Consolidación global pendiente del orquestador, no activación del portal.

---

# Historial: revisión 3, login general

2026-09-14. Franco solicita que el enlace principal de Vercel muestre inicio de sesión, en lugar de abrir el dashboard público. Corrección publicada como I02-R3, deployment `dpl_7pUFo2MZ8vm5jLKEM1ifX7nRBuGi`, READY, target staging.

**Entrada principal: https://nbc-sales-nbc-sales.vercel.app/**. URL inmutable: https://nbc-sales-knuttd71b-nbc-sales.vercel.app/. Anas y Elias conservan sus credenciales y membresías admin. No hay nuevas cuentas ni migraciones.

## Comportamiento

La raíz y todas las páginas, incluida demo, muestran el login sin sesión. El dashboard y su navegación aparecen después de comprobar Auth y membresía activa por `/api/workspace/session`. Members reutiliza la sesión del login general. Sign out cierra el acceso desde el shell o Members. La sesión permanece en memoria durante navegación interna; una pestaña nueva o recarga solicita login de nuevo. No se restaura sesión desde localStorage. APIs privadas mantienen su autorización en servidor; no se incorporó autenticación por una bandera del cliente.

Se integra el trabajo de WorkspaceAccess existente localmente, revisando y congelando doce archivos concretos: componente/CSS de acceso, tipos/servicio/session API, MemberWorkspace, shell/CSS, layout raíz/typography y dos fuentes locales. Se eliminó la excepción pública de `/demo` y se ajustó el texto de login para no prometer integración de Caller que no forma parte de este parche. No se tomaron funcionalidades concurrentes de Caller C02 ni Academy/Lead Engine. El runtime desplegado conserva C01-R1 e I02-R2.

## Procedencia y validación

- Staging `/private/tmp/nbc-login-zhtp63ks`, servidor propio 3114; sin cambios al puerto 3000.
- `npm ci` y build Node 24/Next 16.3.5 pasan. Se preserva package-lock.
- `node scripts/deploy-member-login-check.mjs http://127.0.0.1:3114 local`: **19 checks reales aprobados**. Ocho páginas anónimas muestran login sin navegación; contraseña incorrecta no admite; Anas escritorio/Elias móvil; Members sin segundo login; nueva pestaña/logout; APIs sin token y token inventado rechazados. No mocks ni operaciones pagadas.
- La misma prueba sobre Vercel: **19/19 aprobados**, registrados en `artifacts/lanes/I02/login/vercel-checks.json`; capturas finales `vercel-login-desktop.png` y `vercel-login-mobile.png`. No capturas con passwords.
- Manifiesto de 75 archivos `artifacts/lanes/I02/login/runtime-manifest.json`, digest JSON normalizado `b0df71b381cf86ff2810fe969308cd81f1f1492090b6e6b750f1766ab47289c6`. Archivo reproducible/hash en `runtime-archive.json`. Escaneo de cliente sin valores privados en `security.json`.
- Se reutilizan variables preview; excepción de protección solo para el deployment. Login NBC permanece obligatorio. Push no ejecutado; publicación autorizada como corrección del portal por la instrucción vigente de Franco.

## Deltas para consolidar

API_DOCS: GET `/api/workspace/session`, Bearer verificado por Auth y tabla de membresías; 200 `{user:{id,name,role}}`, 401 token ausente/inválido, 403 membresía inactiva/no permitida, 503 almacenamiento; no-store. Conserva bootstrap explícito del operador existente; membresía suspendida nunca obtiene fallback. DB_SCHEMA sin cambios. Arquitectura/lookup: proveedor de sesión en layout raíz y Members/shell como consumidores; autorización de las APIs sigue independiente. CHANGELOG: FIXED entrada pública del dashboard, ahora login obligatorio en todas las páginas, revisión I02-R3 con evidencia anterior. Handoff y despliegue actualizados; docs globales a consolidar por orquestador.

---

# Historial: revisión 2, publicado y accesos activos

Estado vigente, 2026-09-14: **PUBLICADO EN VERCEL; ANAS Y ELIAS ACTIVOS COMO ADMINS**. La revisión 1 que sigue queda como historial previo a la autorización explícita de activación.

## Resultado de activación

- Portal: **https://nbc-sales-nbc-sales.vercel.app/members**.
- URL inmutable: https://nbc-sales-kopfa2980-nbc-sales.vercel.app/members.
- Deployment `dpl_GyLb1ocx3PZksRSW2nvE7GTn8RDi`, proyecto NBC Sales, READY, preview `target: staging`. Las dos URLs responden 200 sin login Vercel. Excepción de protección limitada a este deployment; autenticación de aplicación intacta.
- Migración `202609140030_members.sql`: **APLICADA Y VERIFICADA en Supabase real**, ocho tablas con RLS. Activadas membresías admin de Anas Daoud y Elias Castañeda; login real de ambos probado sobre Vercel.
- Onboarding, calendario, conversación con mentor, tickets y wallet/ledger persisten en DB. Verificados con tres cuentas temporales eliminadas al finalizar junto con todos sus datos. No se modificaron perfiles ni saldos de Anas/Elias para simular actividad.
- Credenciales iniciales para Franco en archivo privado local 0600; ruta registrada en `artifacts/lanes/I02/activation/credential-handoff.json`. Sin contraseñas en evidencia/código/deploy; sin emails enviados.

## Autorización y procedencia

Instrucción posterior de Franco: «ponélo arriba a Vercel [...] Hace todo lo que tengas que hacer para activarlo». Autoriza la activación SQL/membresías y publicación I02, sustituyendo la espera de aprobación descrita en revisión 1. No se inventa revisión del orquestador. Push no ejecutado: publicación por API autorizada explícitamente.

Se detectó Caller C01-R1 ya publicado en el mismo proyecto. Para conservarlo, se partió de su snapshot verificado `ae073f13ac5ff86b23f9c4eb99a19be88de7b8aafaab48e434049e495701a3e6` y se añadieron los nueve archivos runtime I02. No se tomaron archivos concurrentes del workspace. Build nuevo en `/private/tmp/nbc-members-live-1mz652re`, Node 24/Next 16.3.5, sin cambios al package-lock.

Manifiesto runtime de 67 archivos: `artifacts/lanes/I02/activation/runtime-manifest.json`; digest JSON normalizado enviado a Vercel `ed80cc8acc4b6cc1b2b7cf82c89b66bf96b2daa2e03b5521f7bda455ee55ce2d`. Archivo reproducible `nbc-sales-C01-R1-plus-I02-R2.tar.gz` y su hash en `runtime-archive.json`. SQL conservó el hash del candidato revisado. Se reutilizaron las seis variables runtime preview; no se subieron `.env`, scripts, tests, documentos, SQL ni contraseñas.

## Pruebas reales de la revisión 2

| Comprobación | Resultado | Evidencia bajo `artifacts/lanes/I02/activation/` |
|---|---|---|
| `npm ci`; build producción Node 24 del snapshot combinado | Pasa, incluye ruta Members y reconcile de C01 | `build.log` |
| Aplicación SQL y ocho tablas RLS | HTTP 201 y RLS habilitada | `migration.json`, `schema-check.json` |
| Activación de dos admins | Ambos activos, workspace 200 | `accounts.json` |
| `node scripts/deploy-member-browser.mjs https://nbc-sales-nbc-sales.vercel.app` | 14 checks reales; Anas escritorio, Elias móvil, cinco secciones, salida limpia, sin errores JS/overflow | `browser-live.json`, `browser.log`, `public-desktop.png`, `public-mobile.png` |
| `node scripts/deploy-member-verify-live.mjs https://nbc-sales-nbc-sales.vercel.app` | 12 checks de Auth/API/DB; sin interceptar datos | `live-persistence.json`, `persistence.log` |
| Acceso público HTTPS | URL inmutable y alias 200; APIs anónimas 401 | `access-final.json`, `browser-live.json` |
| Procedencia y secretos | Archivos verificados; escaneo sin valores privados | `security-final.json` |

La prueba de persistencia cubre onboarding guardado/recuperado, acceso de coach asignado y rechazo cruzado, calendario privado, mensaje con reintento único, ticket/resolución, grant admin/rechazo estudiante/reintento, dos reservas **concurrentes por PostgREST** de 70 sobre saldo 100 (exactamente una aceptada), liquidación y liberación con reintentos. Las ocho tablas y RPC rechazan acceso directo de authenticated. Son requests concurrentes reales, no la simulación PGlite anterior. Cleanup: tres cuentas temporales y sus registros eliminados. No hay cargos, llamadas ni actividad falsa persistida para los admins.

Primer chequeo inmediato después de la excepción Vercel aún devolvió 302 por propagación; comprobación final confirma 200 en ambas URLs. No se cambió protección global del proyecto.

## Cómo entrar ahora

Abrir https://nbc-sales-nbc-sales.vercel.app/members, ingresar con email y contraseña inicial del archivo privado. Anas y Elias ven el rol Administrator, su roadmap y los tabs Calendar, Mentor chat, Support y NBC credits. Los calendarios/saldos comienzan sin datos inventados. No se necesita localhost. Las contraseñas no fueron enviadas por email.

## Límites que siguen siendo de producto

Las compras, recargas, créditos incluidos y consumo automático de herramientas requieren definir política comercial y hooks; no se inventaron precios. Chat es una conversación por estudiante con actualización manual, sin canales/adjuntos/notificaciones de Slack. Los eventos y asignaciones de coach reales se cargan por el equipo. Caller conserva su autorización de operador: activar admin de Members no modifica permisos de los módulos ajenos. C01 publicado se conserva íntegro. La consolidación de DB_SCHEMA/API_DOCS/CHANGELOG globales por el orquestador sigue pendiente documental, **no bloquea los accesos ya activos**.

### Delta AInnovate para consolidar

2026-09-14 — DEPLOYED/ACTIVATED I02-R2: Members en Vercel, migración real y dos admins activos por instrucción explícita de Franco. Cambiar estados anteriores de migración/activación a APLICADA Y VERIFICADA; registrar URL/ID/manifiesto/pruebas arriba. Feature, handoff y deployment docs actualizados por Infra. Se añadieron `scripts/deploy-member-publish.mjs`, `deploy-member-verify-live.mjs`, `deploy-member-browser.mjs`; scripts no desplegados. No push, compra ni envío de invitaciones.

---

# Historial: revisión 1, previa a activación

Fecha: 2026-09-14. Lane Infraestructura + Dashboard.
Estado: **PARCIAL CON DEPENDENCIA — candidato local listo para revisión**.
Objetivo: primera base de membresía NBC con admin, coach y estudiante; onboarding, calendario, mentor chat, tickets y créditos; preparar accesos de Anas y Elias.
Candidato: `artifacts/lanes/I02/candidate-manifest.json`, hashes SHA256 de archivos propios. Sin Git. Runtime verificado en `/private/tmp/nbc-i02-e3t95s9u`, baseline I01 SHA256 `bfec999cee4d87329b42e45ed54fcbe1fcea0deefbc0deb24fa28cc131489283` + archivos I02. No se incorporaron cambios concurrentes de otros lanes al build.

## Resultado

Nuevo espacio `/members`, diseño NBC en inglés, sesión en memoria y autorización de servidor basada en membresía activa. Estudiante accede a sus datos; coach a estudiantes asignados; admin supervisa miembros y asigna coach. Onboarding de tres pasos guarda borrador o completa explícitamente. Calendario permite cargar sesiones grupales (admin) o privadas (coach/admin) con enlaces HTTPS. Mentor chat guarda texto e historial paginado con actualización manual. Tickets propios se abren, resuelven y reabren. Créditos muestran saldo disponible/reservado e historial; admin puede registrar una asignación manual. SQL prepara reserva, liquidación y liberación idempotentes.

El código de persistencia está implementado, pero **la migración no está aplicada al Supabase compartido**. Las capturas autenticadas usan datos DEMO interceptados. No se afirma que el portal esté operativo públicamente. No hay checkout, cobro, créditos incluidos definidos, consumo conectado a herramientas, calendario sincronizado, notificaciones ni equivalencia completa con Slack. Onboarding no bloquea todavía los módulos existentes. Branding NBC en el espacio nuevo; eliminación de proveedores/prompts de módulos ajenos pendiente de sus propietarios.

**Cuentas reales:** Anas Daoud y Elias Castañeda fueron creados en Supabase Auth con los emails proporcionados y login comprobado. Roles solicitados: admin. Membresías activadas: **0**, pendientes de esquema/integración. Ninguna invitación enviada. Contraseñas iniciales aleatorias solo en archivo privado 0600 dentro de directorio 0700, localizable mediante `artifacts/lanes/I02/private-access-location.txt`; ese archivo no contiene contraseñas. No incluir el archivo privado en commits, despliegues ni reportes. El baseline publicado conserva la autorización del operador; estas cuentas aún no habilitan sus herramientas privadas.

## Archivos

- `src/lib/member-types.ts`, `member-validation.ts`: contratos, validación y límites de acceso.
- `src/services/member-workspace.ts`, `src/app/api/members/route.ts`: lectura y mutaciones autenticadas, validación y errores saneados.
- `src/app/(workspace)/members/page.tsx`, `src/components/members/{MemberWorkspace,MemberPanels}.tsx`, `Members.module.css`: entrada, paneles y estilos responsive.
- `src/components/platform/PlatformShell.tsx`: enlace Members; única modificación de runtime preexistente.
- `supabase/migrations/202609140030_members.sql`: ocho tablas y función de créditos; nueva asignación de Franco habilita preparar esta propuesta, aplicación compartida reservada a integración coordinada.
- `scripts/deploy-member-provision.mjs`: crear Auth o activar membresías desde input privado; reutiliza cuentas y rechaza sobrescribir roles/status distintos.
- `tests/member-*`: validación, servicio, SQL aislado y navegador; docs de feature, handoff e I02; evidencia bajo `artifacts/lanes/I02/`.

Sin cambios a Caller, Academy, Lead Engine, Ask Anas, helper común de auth, dependencias de aplicación ni docs globales.

## Evidencia y autorrevisión

Entorno: macOS, Node 24.21.0, Next 16.3.5, staging/puerto 3102 propios. Puerto 3000 compartido intacto. Comandos desde raíz salvo build en staging; PGlite instalado exclusivamente como herramienta temporal.

| Comando / comprobación | Resultado | Evidencia |
|---|---|---|
| `npm ci`; `npm exec --yes --package=node@24 -- node node_modules/next/dist/bin/next build` en staging | Build producción y TypeScript pasan | `npm-ci.log`, `build-final.log` |
| `npm exec --yes --package=node@24 -- node --test tests/member-validation.test.ts` | 9/9 | `unit-tests.log` |
| `node --test tests/member-service.test.mjs` | 8/8, transporte Supabase simulado; código de servicio real | `service-tests.log` |
| `node tests/member-pglite.mjs /private/tmp/nbc-i02-sql-20260914` | DDL/PLpgSQL, créditos/reintentos/permisos pasan | `sql-tests-final.log` |
| `node tests/member-browser.mjs /private/tmp/nbc-i02-e3t95s9u artifacts/lanes/I02/browser` | 13 checks; desktop 1440/móvil 390; sin overflow ni errores JS | `browser-final.log`, `browser/browser-result.json` |
| `node tests/member-staff-browser.mjs /private/tmp/nbc-i02-e3t95s9u artifacts/lanes/I02/browser` | 2 recorridos admin/coach | `staff-browser.log`, `browser/staff-result.json` |
| Auth real Anas/Elias + API local | Login correcto ambos; Members 503 por esquema ausente | `account-status.json`, `account-login-check.json` |
| Escaneo cliente y candidato | Resultado y conteos sin valores secretos | `security-check.json` |

Navegador: login/datos/mutaciones de paneles interceptados DEMO; API anónima real GET y POST retorna 401. Se verifican onboarding explícito, borrador conservado ante 503, chat, tickets, destinatario al otorgar créditos y controles por rol, y salida que limpia panel privado. Servicio prueba rechazo de metadata con rol falso, membresía suspendida/no confirmada, acceso cruzado y cambio de owner. SQL prueba grant/retry, prohibición al estudiante, fondos insuficientes, settle parcial/retry, release/retry y permisos. **No se probó concurrencia con conexiones PostgreSQL independientes ni PostgREST real sobre estas tablas.**

Fallos iniciales corregidos: inferencia TypeScript de resultados heterogéneos; paréntesis CASE en PL/pgSQL; ID de ticket conservado al cambiar estado; envío accidental al avanzar al último paso del onboarding. Selectores de test ajustados y capturas reposicionadas arriba. Logs iniciales y captura `DEMO-failure.png` se conservan como historia, no representan resultado final. Autorrevisión de auth, propiedad, errores, SQL y UI completada dentro de este alcance.

## Cómo lo revisa Franco

1. Abrir `http://127.0.0.1:3102/members` en esta máquina mientras corre staging: entrada pública real y enlace Members del shell. La URL HTTPS de I01 no incluye este candidato.
2. Revisar `artifacts/lanes/I02/browser/desktop-public-entry.png` y `mobile-public-entry.png`; luego capturas `desktop-DEMO-*`, `mobile-DEMO-*`, `DEMO-admin.png` y `DEMO-coach.png`. Son demostraciones de interfaz con datos sintéticos identificados.
3. Reproducir los dos scripts de navegador: el primero simula un guardado fallido, mantiene el borrador y completa solo con clic explícito; el segundo comprueba calendario y créditos según rol. No se ofrece selector público que suplante roles.
4. No distribuir las contraseñas como acceso funcional todavía. El login Auth ya pasa, pero panel Members devuelve 503 hasta integrar la migración. Tras integración se debe repetir el recorrido con cuentas reales y después revisar la publicación.

## Datos, APIs y configuración

Migración **PROBADA AISLADA**, no aplicada al proyecto compartido. `nbc_members` referencia `auth.users`; role admin/coach/student, status active/suspended y coach_id. Onboarding por miembro, calendario por destinatario opcional, mensajes por estudiante/autor, tickets por miembro. Wallet por miembro con `0 <= reserved <= balance <= 1000000000`. Operaciones idempotentes y ledger único por miembro/operación; unidades enteras acotadas a 1000000 por operación. Índices por fecha y miembro. Las ocho tablas tienen RLS y acceso anon/authenticated revocado; service_role opera únicamente tras autorización de API. Actualización de ledger revocada a service_role. SQL exacto de políticas, constraints y grants en la migración.

`nbc_credit_apply(p_action text,p_member uuid,p_operation uuid,p_amount bigint,p_reason text,p_actor uuid)` es SECURITY INVOKER con EXECUTE solo service_role. Bloquea wallet `FOR UPDATE`; acciones grant/reserve/settle/release; rechaza payload distinto al reusar operación y transiciones incompatibles. Grant exige actor admin; usage exige actor=miembro. No se expone reserva/liquidación directamente al navegador. La prueba de concurrencia pendiente: dos conexiones reservan 70 sobre saldo 100; solo una debe lograrlo y ninguna dejar saldo disponible negativo.

GET `/api/members`: Bearer de Supabase validado vía getUser + membresía activa. Query opcional `member=<uuid>` y `before=<timestamp>|<uuid>`. Retorna `{member,subject,people,onboarding,events,messages,tickets,credits:{available,reserved,entries},nextMessages}`. Mensajes 30 por página; miembros/eventos máximo 100, tickets/ledger últimos 50. Solo mensajes tienen paginación; escalar listados restantes queda pendiente.

POST `/api/members`: mismo auth, JSON máximo 8192 bytes; retorna `{saved:true}`. Acciones:

| action | Campos además de action | Permiso |
|---|---|---|
| onboarding | business, timezone IANA, goal, questions, complete boolean | Solo propio; completar exige negocio/objetivo |
| event | id UUID, memberId opcional, title, startsAt/endsAt ISO, joinUrl HTTPS | Admin grupo; coach/admin estudiante autorizado |
| assignCoach | memberId, coachId UUID | Admin; estudiante y coach activo |
| message | id UUID, memberId opcional, body | Estudiante propio o equipo autorizado; requiere coach asignado |
| ticket | id UUID, title, body | Propio |
| ticketStatus | id UUID, memberId opcional, status open/resolved | Propio o equipo autorizado |
| grant | id UUID, memberId opcional, amount entero, reason | Admin |

Errores `{error:string}`: 400 entrada inválida, 401 sesión ausente/no válida, 403 permiso/membresía, 404 recurso fuera de alcance, 409 conflicto, 413 tamaño, 415 content-type, 503 persistencia/configuración, 500 inesperado saneado. Respuestas no-store. Mensajes/tickets/grants conservan UUID en reintentos; evento duplicado devuelve conflicto para refrescar. Sin HTML ejecutable en mensajes/preguntas. No se retorna prompt/modelo/proveedor desde esta API.

Variables nuevas necesarias: ninguna. Usa `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SECRET_KEY`. Legacy conserva `NBC_OPERATOR_EMAIL`, `ELEVENLABS_API_KEY`, `ELEVENLABS_AGENT_ID`. No incluir secretos administrativos en build cliente. Operaciones cloud realizadas: creación y verificación Auth de dos cuentas; no invitaciones, llamadas ni pagos. Facturación de infraestructura no auditada.

Activación coordinada pendiente: revisar/aplicar migración y pruebas reales; ejecutar `node scripts/deploy-member-provision.mjs PRIVATE_INPUT PRIVATE_NEW_OUTPUT --apply-memberships` con el input privado existente y archivo de salida nuevo, permisos 0600. El modo reutiliza usuarios y no cambia password. Luego comprobar ambos roles desde servidor. Se debe definir entrega segura de acceso y recuperación de contraseña antes de distribuir cuentas a miembros. No se creó pantalla de administración masiva de usuarios.

## Documentación AInnovate

Feature `docs/features/member-platform.md` y handoff `docs/lanes/infra.md` actualizados. Docs globales pendientes del orquestador.

### Entrada propuesta para CHANGELOG

2026-09-14 — ADDED: candidato I02 Members con roles de servidor, onboarding, calendario, mentor chat, tickets y wallet/ledger NBC; archivos del manifiesto I02. CHANGED: shell añade Members. Request: «crear los roles [...] admins, coaches y estudiantes», «Un tab de calendario», «Una función de chat», «abrir tickets», «Empecemos también con lo de los créditos [...] metele un onboarding a todo». Auth de Anas/Elias creado a pedido; activación admin y publicación pendientes. SQL solo probado aislado; sin cobros ni integración de permisos en módulos ajenos.

### Deltas exactos pendientes de consolidar

- DB_SCHEMA: incorporar las ocho tablas y RPC anteriores, FKs/checks/índices/grants de `202609140030_members.sql`; estado PROBADA AISLADA hasta evidencia real.
- API_DOCS: agregar GET/POST `/api/members`, acciones, payloads, límites, roles y errores de este reporte. Mantener auth legacy distinguida.
- Arquitectura y lookup: nueva capa `member-workspace` server-only, contratos/validadores, dos componentes y CSS Module, ruta Members; separación Auth/membership y sesión cliente en memoria.
- Seguridad: rol autoritativo en tabla, service_role detrás de autorización, datos privados no-store, RLS sin acceso cliente; integración global y pruebas concurrentes pendientes.
- Decisiones/producto: moneda NBC sin conversión monetaria ni precios aprobados; calendario manual y chat de una conversación por estudiante; no se vende como reemplazo completo de Slack. Documentar activación por compra cuando exista contrato.

## Criterios de aceptación y dependencias

| Criterio | Estado | Dependencia |
|---|---|---|
| Roles, aislamiento, onboarding, calendario/chat/tickets y UI | Implementados y probados localmente | Persistencia integrada + revisión real |
| Anas y Elias admin | Auth real creado; activación pendiente | Orquestador aplica SQL y modo membresías |
| Créditos | Ledger/RPC y UI candidatos | Concurrencia nativa, precios y hooks de consumo |
| Acceso incluido con compra | Pendiente | Señal de compra confiable, ciclo de membresía y checkout |
| Plataforma NBC sin detalles internos visibles | Nuevo espacio cubierto | Revisar módulos de otros lanes y contratos de herramientas |
| Calendario y coaching reales | Estructura lista | Horarios, enlaces y asignaciones reales |
| HTTPS del nuevo portal | Pendiente | Revisión técnica + Franco; nuevo despliegue autorizado |

## Estado de revisión y publicación

- Autorrevisión: completada con límites de evidencia detallados.
- Revisión técnica del orquestador: **PENDIENTE**, incluyendo SQL compartido e integración con lanes.
- Revisión de Franco: **PENDIENTE** para I02. Aprobación visual del baseline anterior no cubre este código nuevo.
- Push: **NO EJECUTADO**.
- Deploy I02: **NO EJECUTADO**. Baseline I01 permanece publicado en `https://nbc-sales-nyqgilywg-nbc-sales.vercel.app`.
- Candidato propuesto: manifiesto I02; destino posterior preview de Vercel `nbc-sales`, `target: staging`, una vez integrado y aprobado. El manifiesto cambia si la integración modifica archivos; esa versión requiere su propia revisión.

La revisión previa proviene de la instrucción de Franco «Antes de pushear cualquier cosa, yo reviso y ahí pushean» y [REVIEW-PROTOCOL.md](../REVIEW-PROTOCOL.md). No se solicita permiso para el trabajo local ya completado ni se toma la creación de cuentas como autorización de publicar código nuevo.

## Próxima tarea sugerida

Integrar I02 de forma coordinada: SQL y concurrencia, activar los dos admins y comprobar persistencia real antes de presentar la versión publicable. Posteriormente, contrato de créditos y autorización común con propietarios de herramientas. No iniciado desde este lane.
